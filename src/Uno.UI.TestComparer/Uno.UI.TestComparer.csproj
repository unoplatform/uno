<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<OutputType>Exe</OutputType>
		<TargetFramework>$(NetPrevious)-windows</TargetFramework>
		<RollForward>Major</RollForward>
		<AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
		<UseWPF>true</UseWPF>
		<!-- NU1903: SQLitePCLRaw.lib.e_sqlite3 (GHSA-2m69-gcr7-jv3q), transitive via Microsoft.AppCenter;
		     no unaffected replacement version is available yet. -->
		<NoWarn>SYSLIB0014;MSB3277;NU1903</NoWarn>
	</PropertyGroup>

	<ItemGroup>
		<None Remove="Properties\launchSettings.json" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.AppCenter" />
		<PackageReference Include="Microsoft.VisualStudio.Services.InteractiveClient" />
		<PackageReference Include="Microsoft.TeamFoundationServer.Client" />
		<PackageReference Include="Refit" />
		<PackageReference Include="Mono.Options" />

		<!-- BEGIN WORKAROUND: These dependencies already come transitively, but the version that comes transitively has security vulnerabilities reported by NuGet -->
		<PackageReference Include="Microsoft.IdentityModel.JsonWebTokens" />
		<PackageReference Include="System.Data.SqlClient" />
		<PackageReference Include="System.IdentityModel.Tokens.Jwt" />
		<!-- END WORKAROUND -->
	</ItemGroup>

	<ItemGroup>
		<Folder Include="Properties\" />
	</ItemGroup>

</Project>
