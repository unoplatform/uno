<Project>
	<!--
		Keep AssemblyName isolated so the build-time SHA substitution can rewrite this file
		without dirtifying the csproj, which is part of the NuGet cache key.
	-->
	<PropertyGroup>
		<AssemblyName>Uno.UI.Tasks.v0</AssemblyName>
	</PropertyGroup>
</Project>
