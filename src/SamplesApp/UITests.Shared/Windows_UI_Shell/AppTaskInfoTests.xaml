<Page
    x:Class="UITests.Windows_UI_Shell.AppTaskInfoTests"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="16" Spacing="12">
            <TextBlock
                FontSize="20"
                FontWeight="SemiBold"
                Text="Windows.UI.Shell.Tasks AppTaskInfo" />
            <TextBlock Text="Create and update app tasks, then inspect the app icon while this page is open. Win32 maps task state to taskbar progress, while macOS maps the number of running tasks to the Dock badge." TextWrapping="Wrap" />
            <TextBlock
                x:Name="SupportTextBlock"
                FontWeight="SemiBold"
                TextWrapping="Wrap" />

            <StackPanel x:Name="ControlsPanel" Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Click="CreatePrimaryTask_Click" Content="Create primary task" />
                    <Button Click="AdvancePrimaryTask_Click" Content="Advance step" />
                    <Button Click="AddSecondaryTask_Click" Content="Add extra task" />
                    <Button Click="RefreshTasks_Click" Content="Refresh task list" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Click="PausePrimaryTask_Click" Content="Pause" />
                    <Button Click="NeedsAttentionPrimaryTask_Click" Content="Needs attention" />
                    <Button Click="ResumePrimaryTask_Click" Content="Resume" />
                    <Button Click="FailPrimaryTask_Click" Content="Error" />
                    <Button Click="RemovePrimaryTask_Click" Content="Remove primary task" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Click="ShowPreviewContent_Click" Content="Preview content" />
                    <Button Click="ShowGeneratedAssets_Click" Content="Generated assets" />
                    <Button Click="CompletePrimaryTask_Click" Content="Complete task" />
                    <Button Click="ClearAllTasks_Click" Content="Clear all tasks" />
                </StackPanel>
            </StackPanel>

            <TextBlock x:Name="PrimaryTaskTextBlock" TextWrapping="Wrap" />
            <TextBlock x:Name="TasksSummaryTextBlock" FontWeight="SemiBold" />
            <ListView
                x:Name="TasksListView"
                MinHeight="120"
                MaxHeight="220"
                SelectionMode="None" />

            <TextBlock FontWeight="SemiBold" Text="Action log" />
            <ListView
                x:Name="LogListView"
                MinHeight="120"
                MaxHeight="220"
                SelectionMode="None" />
        </StackPanel>
    </ScrollViewer>
</Page>
