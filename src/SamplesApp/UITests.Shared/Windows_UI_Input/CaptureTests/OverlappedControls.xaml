<Page x:Class="UITests.Shared.Windows_UI_Input.CaptureTests.OverlappedControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Input.CaptureTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<Grid>
		<Border x:Name="_orange"
		    Width="200"
		    Height="200"
		    HorizontalAlignment="Center"
		    VerticalAlignment="Center"
		    Background="Orange">
			<Grid>
				<TextBlock Text="This will capture the pointer when pressed" />

				<Border x:Name="_green"
				    Width="50"
				    Height="50"
				    HorizontalAlignment="Center"
				    VerticalAlignment="Center"
				    Background="Green" />
			</Grid>
		</Border>

		<StackPanel VerticalAlignment="Bottom">
			<Border MinWidth="200"
			    Background="Orange">
				<TextBlock x:Name="_orangeOutput"
				    Foreground="Black"
				    Text="Last event: **none**" />
			</Border>
			<Border MinWidth="200"
			    Background="Green">
				<TextBlock x:Name="_greenOutput"
				    Foreground="Black"
				    Text="Last event: **none**" />
			</Border>
		</StackPanel>
	</Grid>
</Page>
