<Page
    x:Class="UITests.Windows_UI_Input.PointersTests.NestedHandling"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Input.PointersTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border
            x:Name="_container"
            Width="300"
            Height="300"
            Background="DeepSkyBlue">
            <Border
                x:Name="_intermediate"
                Width="200"
                Height="200">
                <Border x:Name="_nested" Background="DeepPink" />
            </Border>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Pressed/Release result: " />
            <TextBlock x:Name="_result" Text="**not run**" />
        </StackPanel>

        <Border
            x:Name="_sample2_container"
            Grid.Column="1"
            Width="300"
            Height="300"
            Background="DeepSkyBlue">
            <Border
                x:Name="_sample2_intermediate"
                Width="200"
                Height="200">
                <Border x:Name="_sample2_intermediate2">
                    <Border x:Name="_sample2_nested" Background="DeepPink" />
                </Border>
            </Border>
        </Border>

        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            Orientation="Horizontal">
            <TextBlock Text="Entered result: " />
            <TextBlock x:Name="_enterResult" Text="**not run**" />

            <TextBlock Text=" | Exited result: " />
            <TextBlock x:Name="_exitResult" Text="**not run**" />
        </StackPanel>
    </Grid>
</Page>
