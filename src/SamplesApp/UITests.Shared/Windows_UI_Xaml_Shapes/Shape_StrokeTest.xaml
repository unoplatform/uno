<Page x:Class="UITests.Windows_UI_Xaml_Shapes.Shape_StrokeTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<StackPanel Spacing="16">
		<!--  note: for unknown reason, this Grid is needed for the test to fail before the fix  -->
		<Grid Width="50"
		    Height="50">
			<Rectangle x:Name="TestTarget"
			    Width="40"
			    Height="40"
			    StrokeThickness="20" />
		</Grid>

		<Button x:Name="ChangeThemeButton"
		    Click="{x:Bind ChangeTheme}"
		    Content="ChangeTheme" />
		<Button x:Name="UpdateBrushButton"
		    Click="{x:Bind UpdateBrush}"
		    Content="UpdateBrush" />
		<Button x:Name="UpdateBrushColorButton"
		    Click="{x:Bind UpdateBrushColor}"
		    Content="UpdateBrushColor" />
	</StackPanel>
</Page>
