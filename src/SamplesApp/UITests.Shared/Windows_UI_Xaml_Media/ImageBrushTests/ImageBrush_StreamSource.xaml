<UserControl
	x:Class="Uno.UI.Samples.UITests.ImageBrushTestControl.ImageBrush_StreamSource" 
	xmlns:controls="using:Uno.UI.Samples.Controls"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Uno.UI.Samples.UITests.ImageBrushTestControl"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:u="using:Uno.UI.Samples.Controls"
	xmlns:ios="http://uno.ui/ios"
	xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:android="http://uno.ui/android"
	mc:Ignorable="d ios android"
	d:DesignHeight="2000"
	d:DesignWidth="400">

	<StackPanel Spacing="5">

		<StackPanel Orientation="Horizontal" Spacing="5">
			<Border BorderBrush="Green"
			        BorderThickness="5"
			        CornerRadius="10"
			        Width="150"
			        Height="80">
				<Border.Background>
					<ImageBrush x:Name="imageBrush1"  Stretch="Uniform"
								AlignmentX="Left"
								ImageSource="{x:Bind MySource, Mode=OneWay}" />
				</Border.Background>
			</Border>
			<Border BorderBrush="Green"
			        BorderThickness="5"
			        CornerRadius="10"
			        Width="150"
			        Height="80">
				<Border.Background>
					<ImageBrush x:Name="imageBrush2" Stretch="Uniform"
					            AlignmentX="Center"
					            ImageSource="{x:Bind MySource, Mode=OneWay}" />
				</Border.Background>
			</Border>
			<Border BorderBrush="Green"
			        BorderThickness="5"
			        CornerRadius="10"
			        Width="150"
			        Height="80">
				<Border.Background>
					<ImageBrush x:Name="imageBrush3" Stretch="Uniform"
					            AlignmentX="Right"
					            ImageSource="{x:Bind MySource, Mode=OneWay}" />
				</Border.Background>
			</Border>
		</StackPanel>
		<StackPanel Orientation="Horizontal" Spacing="5">
			<Border BorderBrush="Green"
			        BorderThickness="5"
			        CornerRadius="10"
			        Width="150"
			        Height="80">
				<Border.Background>
					<ImageBrush x:Name="imageBrush4" Stretch="UniformToFill"
					            AlignmentY="Top"
					            ImageSource="{x:Bind MySource, Mode=OneWay}" />
				</Border.Background>
			</Border>
			<Border BorderBrush="Green"
			        BorderThickness="5"
			        CornerRadius="10"
			        Width="150"
			        Height="80">
				<Border.Background>
					<ImageBrush x:Name="imageBrush5" Stretch="UniformToFill"
					            AlignmentY="Center"
					            ImageSource="{x:Bind MySource, Mode=OneWay}" />
				</Border.Background>
			</Border>
			<Border BorderBrush="Green"
			        BorderThickness="5"
			        CornerRadius="10"
			        Width="150"
			        Height="80">
				<Border.Background>
					<ImageBrush x:Name="imageBrush6" Stretch="UniformToFill"
					            AlignmentY="Bottom"
					            ImageSource="{x:Bind MySource, Mode=OneWay}" />
				</Border.Background>
			</Border>
		</StackPanel>
	</StackPanel>
</UserControl>
