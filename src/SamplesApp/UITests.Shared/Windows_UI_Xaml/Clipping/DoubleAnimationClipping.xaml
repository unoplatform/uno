<UserControl
    x:Class="SamplesApp.Windows_UI_Xaml.Clipping.DoubleAnimationClipping"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <ContentControl x:Name="MainContent">
        <ContentControl.Template>
            <ControlTemplate>
                <Grid x:Name="RootGrid" Background="LightGray">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <CommandBar
                        Grid.Row="0"
                        Background="Aqua"
                        Content="Title" />
                    <Border
                        Grid.Row="1"
                        Margin="16"
                        Padding="8,12"
                        Background="Gray">
                        <Rectangle x:Name="SUT" Fill="Red" />
                    </Border>
                    <StackPanel Grid.Row="2">
                        <Button Click="GotoReadyState" Content="GOTO Ready" />
                        <Button Click="GotoNotReadyState" Content="GOTO NotReady" />
                    </StackPanel>
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="ReadyStates">
                            <VisualState x:Name="Ready">
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="SUT"
                                        Storyboard.TargetProperty="Opacity"
                                        To="1"
                                        Duration="0:0:0.5" />
                                </Storyboard>
                            </VisualState>
                            <VisualState x:Name="NotReady">
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="SUT"
                                        Storyboard.TargetProperty="Opacity"
                                        To="0"
                                        Duration="0:0:0.5" />
                                </Storyboard>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                </Grid>
            </ControlTemplate>
        </ContentControl.Template>
    </ContentControl>
</UserControl>
