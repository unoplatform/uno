<Page x:Class="UITests.Windows_UI_Xaml.UIElementTests.UIElement_Layout_Bug2879"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<StackPanel MaxWidth="350"
	    VerticalAlignment="Center"
	    Spacing="8">
		<TextBlock>
			This is an illustration of<Hyperlink NavigateUri="https://github.com/unoplatform/uno/issues/2879">Bug #2879</Hyperlink>
			.
			To trigger the bug, press few times on the follow button:</TextBlock>
		<ToggleButton x:Name="toggle"
		    HorizontalAlignment="Center"
		    Background="LightGray">
			Change State
		</ToggleButton>
		<Grid MinHeight="80"
		    Margin="8,3,8,15"
		    Padding="0,6,0,13"
		    Background="Gray"
		    CornerRadius="13">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<StackPanel Padding="20,5"
			    Background="Gold">
				<TextBlock HorizontalAlignment="Left"
				    FontSize="20"
				    Text="qwertyuiopasdfghjklxcvbnmqwertyu"
				    TextWrapping="Wrap" />
			</StackPanel>

			<StackPanel Grid.Column="1"
			    Width="100"
			    Background="Red"
			    Visibility="{Binding IsChecked, ElementName=toggle}" />
		</Grid>
		<TextBlock>State OFF (should be like this when unpressed)</TextBlock>
		<Grid MinHeight="80"
		    Margin="8,3,8,15"
		    Padding="0,6,0,13"
		    Background="Gray"
		    CornerRadius="13">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<StackPanel Padding="20,5"
			    Background="Gold">
				<TextBlock HorizontalAlignment="Left"
				    FontSize="20"
				    Text="qwertyuiopasdfghjklxcvbnmqwertyu"
				    TextWrapping="Wrap" />
			</StackPanel>

			<StackPanel Grid.Column="1"
			    Width="100"
			    Background="Red"
			    Visibility="Collapsed" />
		</Grid>
		<TextBlock>State ON (should be like this when pressed)</TextBlock>
		<Grid MinHeight="80"
		    Margin="8,3,8,15"
		    Padding="0,6,0,13"
		    Background="Gray"
		    CornerRadius="13">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<StackPanel Padding="20,5"
			    Background="Gold">
				<TextBlock HorizontalAlignment="Left"
				    FontSize="20"
				    Text="qwertyuiopasdfghjklxcvbnmqwertyu"
				    TextWrapping="Wrap" />
			</StackPanel>

			<StackPanel Grid.Column="1"
			    Width="100"
			    Background="Red" />
		</Grid>

	</StackPanel>
</Page>
