<Page
	x:Class="UITests.Windows_UI_Xaml.UIElementTests.UIElement_Elevation"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:toolkit="using:Uno.UI.Toolkit"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<StackPanel Spacing="10" Margin="15">
		<Slider x:Name="elevation1" Minimum="0" Maximum="50" Value="10" />
		<StackPanel Orientation="Horizontal" Spacing="10">
			<Button x:Name="noElevation" Tag="0" Click="SetElevationClick">NO ELEVATION</Button>
			<Button x:Name="lowElevation" Tag="4" Click="SetElevationClick">LOW ELEVATION (4)</Button>
			<Button x:Name="highElevation" Tag="20" Click="SetElevationClick">HIGH ELEVATION (20)</Button>
		</StackPanel>
		<TextBlock FontSize="15">
			ElevatedView, some with rounded corners...
		</TextBlock>
		<StackPanel Orientation="Horizontal" Spacing="10">
			<toolkit:ElevatedView Width="60" Height="60" Background="Orange" Elevation="{Binding Value, ElementName=elevation1}" />
			<toolkit:ElevatedView Width="60" Height="60" Background="Orange" Elevation="{Binding Value, ElementName=elevation1}">
				<Rectangle Fill="Red" />
			</toolkit:ElevatedView>
			<toolkit:ElevatedView Width="60" Height="60" CornerRadius="20" Background="Orange" Elevation="{Binding Value, ElementName=elevation1}">
				<Rectangle Fill="Red" />
			</toolkit:ElevatedView>
			<toolkit:ElevatedView Width="60" Height="60" CornerRadius="10,5,10,35" Background="Orange" Elevation="{Binding Value, ElementName=elevation1}">
				<Rectangle Fill="Red" />
			</toolkit:ElevatedView>
		</StackPanel>
		<TextBlock FondSize="15">
			ElevatedView with Button in them...
		</TextBlock>
		<StackPanel Orientation="Horizontal" Spacing="10">
			<toolkit:ElevatedView Background="Orange" VerticalAlignment="Top" Elevation="{Binding Value, ElementName=elevation1}">
				<Button>Nice Elevated Button</Button>
			</toolkit:ElevatedView>
			<toolkit:ElevatedView Background="White" Elevation="{Binding Value, ElementName=elevation1}">
				<Button Height="120" Width="120">BIG BUTTON</Button>
			</toolkit:ElevatedView>
		</StackPanel>
		<TextBlock FondSize="15">
			ElevatedView Without background (shouldn't be elevated)
		</TextBlock>
		<StackPanel Orientation="Horizontal" Spacing="10">
			<toolkit:ElevatedView Width="60" Height="60" Elevation="{Binding Value, ElementName=elevation1}">
				<Rectangle Fill="Red" />
			</toolkit:ElevatedView>
			<toolkit:ElevatedView Width="60" Height="60" CornerRadius="10,5,10,35" Elevation="{Binding Value, ElementName=elevation1}">
				<Rectangle Fill="Red" />
			</toolkit:ElevatedView>
			<toolkit:ElevatedView VerticalAlignment="Top" Elevation="{Binding Value, ElementName=elevation1}">
				<Button>Nice Elevated Button</Button>
			</toolkit:ElevatedView>
			<toolkit:ElevatedView Elevation="{Binding Value, ElementName=elevation1}">
				<Button Height="120" Width="120">BIG BUTTON</Button>
			</toolkit:ElevatedView>
		</StackPanel>
	</StackPanel>
</Page>
