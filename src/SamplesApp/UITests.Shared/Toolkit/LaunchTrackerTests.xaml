<Page
    x:Class="UITests.Toolkit.LaunchTrackerTests"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITests.Toolkit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Padding="12">
	  <TextBlock>
		<Run Text="Launch count:" FontWeight="Bold" />
		<Run Text="{x:Bind Tracker.LaunchCount}" />
	  </TextBlock>
	  <TextBlock>
		<Run Text="Current launch date:" FontWeight="Bold" />
		<Run Text="{x:Bind Tracker.CurrentLaunchDate}" />
	  </TextBlock>
	  <TextBlock>
		<Run Text="Previous launch date:" FontWeight="Bold" />
		<Run Text="{x:Bind Tracker.PreviousLaunchDate}" />
	  </TextBlock>
	  <TextBlock>
		<Run Text="Is first launch:" FontWeight="Bold" />
		<Run Text="{x:Bind Tracker.IsFirstLaunch}" />
	  </TextBlock>
	  <TextBlock>
		<Run Text="Is first launch for version:" FontWeight="Bold" />
		<Run Text="{x:Bind Tracker.IsFirstLaunchForCurrentVersion}" />
	  </TextBlock>
	  <TextBlock>
		<Run Text="Is first launch for build:" FontWeight="Bold" />
		<Run Text="{x:Bind Tracker.IsFirstLaunchForCurrentBuild}" />
	  </TextBlock>
    </StackPanel>
</Page>
