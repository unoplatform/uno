<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="WindowingSamples.ExtendContentIntoTitleBarWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WindowingSamples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Custom title bar with extends content into title bar  -->
        <Grid
            x:Name="CustomTitleBar"
            Grid.Row="0"
            Height="48"
            Background="Beige">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <FontIcon
                Grid.Column="0"
                Margin="16,0,8,0"
                VerticalAlignment="Center"
                FontSize="20"
                Glyph="&#xE15B;" />
            <TextBlock
                Grid.Column="1"
                VerticalAlignment="Center"
                FontWeight="SemiBold"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="Extended Title Bar" />
        </Grid>

        <StackPanel
            Grid.Row="1"
            Padding="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16">
            <TextBlock
                MaxWidth="600"
                HorizontalAlignment="Center"
                FontSize="18"
                TextWrapping="Wrap">
                <Run FontWeight="Bold">ExtendsContentIntoTitleBar Demo</Run>
                <LineBreak />
                <LineBreak />
                This window demonstrates the ExtendsContentIntoTitleBar API which allows extending XAML UI into the title bar area while preserving the caption buttons (minimize, maximize, close).<LineBreak />
                <LineBreak />
                Notice the custom title bar at the top with the icon, text, and logo - this area is part of the window's content but appears in the title bar region.</TextBlock>

            <Border
                MaxWidth="600"
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Spacing="8">
                    <TextBlock
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="Key Features:" />
                    <TextBlock TextWrapping="Wrap">• Custom UI in title bar area</TextBlock>
                    <TextBlock TextWrapping="Wrap">• System caption buttons remain visible and functional</TextBlock>
                    <TextBlock TextWrapping="Wrap">• Supports drag-to-move from title bar</TextBlock>
                    <TextBlock TextWrapping="Wrap">• Maintains native window behaviors</TextBlock>
                </StackPanel>
            </Border>

            <Button HorizontalAlignment="Center" Click="CloseClick">Close Window</Button>
        </StackPanel>
    </Grid>
</Window>
