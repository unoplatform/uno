<UserControl
    x:Class="UITests.Shared.Windows_Devices.GeolocatorTests"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITests.Shared.Windows_Devices"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

	<StackPanel>
		<Button Command="{Binding RequestAccessCommand}">Request access</Button>
		<TextBlock>
			<Run FontWeight="Bold">Request: </Run>
			<Run Text="{Binding GeolocationAccessStatus}" />
		</TextBlock>
		<Button Command="{Binding GetGeopositionCommand}">Get geoposition</Button>
	  <GeopositionDisplayControl Geoposition="{x:Bind Geoposition, Mode=OneWay}" />
	</StackPanel>
</UserControl>
