<UserControl
    x:Class="UITests.Shared.Windows_Devices.AccelerometerTests"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITests.Shared.Windows_Devices"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>
		<TextBlock Text="Last reading" Style="{ThemeResource SubtitleTextBlockStyle}" />
		<TextBlock>
			<Run FontWeight="Bold">AccelerationX: </Run>
			<Run Text="{Binding LastReading.AccelerationX}" />
		</TextBlock>
		<TextBlock>
			<Run FontWeight="Bold">AccelerationY: </Run>
			<Run Text="{Binding LastReading.AccelerationY}" />
		</TextBlock>
		<TextBlock>
			<Run FontWeight="Bold">AccelerationZ: </Run>
			<Run Text="{Binding LastReading.AccelerationZ}" />
		</TextBlock>
		<TextBlock>
			<Run FontWeight="Bold">Timestamp: </Run>
			<Run Text="{Binding LastReading.Timestamp}" />
		</TextBlock>
		<TextBlock Text="Last shaken" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,20,0,0" />
		<TextBlock>
			<Run FontWeight="Bold">Timestamp: </Run>
			<Run Text="{Binding LastShake}" />
		</TextBlock>
	</StackPanel>
</UserControl>
