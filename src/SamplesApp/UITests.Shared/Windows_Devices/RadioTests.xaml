<UserControl x:Class="UITests.Shared.Windows_Devices.RadioTests"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_Devices"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

	<ScrollViewer>
		<StackPanel>
			<TextBlock Text="Get device's Radios" />
			<Button Click="uiCreate_Click"
			    Content="Invoke GetRadiosAsync" />
			<TextBlock Name="uiResultMsg"
			    Text="" />

			<ListView x:Name="uiListItems"
			    Grid.Row="1"
			    Margin="2,5,5,5"
			    HorizontalContentAlignment="Stretch">
				<ListView.ItemContainerStyle>
					<Style TargetType="ListViewItem">
						<Setter Property="HorizontalContentAlignment" Value="Stretch" />
					</Style>
				</ListView.ItemContainerStyle>

				<ListView.ItemTemplate>
					<DataTemplate>
						<Grid Margin="0,5,0,0"
						    HorizontalAlignment="Stretch"
						    BorderBrush="Blue"
						    BorderThickness="2">
							<Grid.ColumnDefinitions>
								<ColumnDefinition />
								<ColumnDefinition />
								<ColumnDefinition />
							</Grid.ColumnDefinitions>

							<TextBlock Grid.Column="0"
							    Text="{Binding RadioName, Mode=OneWay}" />
							<TextBlock Grid.Column="1"
							    Text="{Binding RadioKind, Mode=OneWay}" />
							<TextBlock Grid.Column="2"
							    Text="{Binding RadioState, Mode=OneWay}" />


						</Grid>

					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>

		</StackPanel>
	</ScrollViewer>
</UserControl>
