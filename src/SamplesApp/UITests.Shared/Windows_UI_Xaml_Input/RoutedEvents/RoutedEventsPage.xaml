<Page
	x:Class="SamplesApp.Samples.RoutedEvents.RoutedEventsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid Margin="50" x:Name="outer" Background="Aqua">
		<Border MaxHeight="50" Background="#48000000" VerticalAlignment="Top">
			<TextBlock x:Name="resultOuter" TextWrapping="Wrap" MaxHeight="50" />
		</Border>
		<Grid Margin="50" x:Name="middle" Background="Bisque">
			<Border MaxHeight="50" Background="#48000000" VerticalAlignment="Top">
				<TextBlock x:Name="resultMiddle" TextWrapping="Wrap" MaxHeight="50" />
			</Border>
			<Grid Margin="50" MinWidth="200" MinHeight="200" x:Name="inner" Background="DeepSkyBlue">
				<Border MaxHeight="50" Background="#48000000" VerticalAlignment="Top">
					<TextBlock x:Name="resultInner" TextWrapping="Wrap" MaxHeight="50" />
				</Border>
				<ScrollViewer Margin="30">
					<Grid Margin="50" MinWidth="200" MinHeight="200" x:Name="scroll" Background="YellowGreen">
						<Border MaxHeight="50" Background="#48000000" VerticalAlignment="Top">
							<TextBlock x:Name="resultScroll" TextWrapping="Wrap" MaxHeight="50" />
						</Border>
						<Button x:Name="btn">You can focus this button</Button>
						<TextBox VerticalAlignment="Bottom" />
						<Slider VerticalAlignment="Bottom" Margin="0, 40" Value="0.5" Minimum="-1" Maximum="1.5" StepFrequency="0.1" />
					</Grid>
				</ScrollViewer>
				<TextBox VerticalAlignment="Bottom" />
			</Grid>
			<TextBox VerticalAlignment="Bottom" />
		</Grid>
		<TextBox VerticalAlignment="Bottom" />
	</Grid>
</Page>
