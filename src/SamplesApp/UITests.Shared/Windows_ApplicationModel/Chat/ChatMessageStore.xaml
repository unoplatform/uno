<UserControl
    x:Class="UITests.Windows_ApplicationModel.Chat.ChatMessageStore"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITests.Windows_ApplicationModel.Chat"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<StackPanel Padding="20">
			<Button Click="getMessages_Click">Get messages</Button>
			<TextBlock Text="" Foreground="Red" Name="uiErrorMsg"/>
			<TextBlock Text="Can take some time ... " Name="uiOkMsg"/>
		</StackPanel>
		<ListView x:Name="uiListItems" Grid.Row="1" Margin="2,5,2,5" HorizontalContentAlignment="Stretch" >
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="HorizontalContentAlignment"  Value="Stretch"/>
				</Style>
			</ListView.ItemContainerStyle>

			<ListView.ItemTemplate>
				<DataTemplate>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<TextBlock Grid.Column="0" Text="{Binding direction, Mode=OneWay }"/>
						<TextBlock Grid.Column="1" Text="{Binding timeStamp, Mode=OneWay }"/>
						<TextBlock Grid.Column="2" Text="{Binding sender, Mode=OneWay }"/>
						<TextBlock Grid.Column="3" Text="{Binding recipient, Mode=OneWay }"/>
						<TextBlock Grid.Column="4" Text="{Binding body, Mode=OneWay }"/>
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</UserControl>
