<Page
    x:Class="UITests.Windows_ApplicationModel.Contacts.ContactReader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_ApplicationModel.Contacts"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
    	<StackPanel Padding="20">
		   <Button Click="getContacts_Click">Get contact</Button>
		   <TextBlock Text="" Foreground="Red" Name="uiErrorMsg"/>
		   <TextBlock Text="Can take some time if you have many contacts defined" Name="uiOkMsg"/>
	    </StackPanel>
		<ListView x:Name="uiListItems" Grid.Row="1" Margin="2,5,2,5" HorizontalContentAlignment="Stretch" >
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="HorizontalContentAlignment"  Value="Stretch"/>
				</Style>
			</ListView.ItemContainerStyle>

			<ListView.ItemTemplate>
				<DataTemplate>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition />
							<ColumnDefinition />
							<ColumnDefinition  />
						</Grid.ColumnDefinitions>
						<TextBlock Grid.Column="0" Text="{Binding fullName, Mode=OneWay }"/>
						<TextBlock Grid.Column="1" Text="{Binding phone, Mode=OneWay }"/>
						<TextBlock Grid.Column="2" Text="{Binding email, Mode=OneWay }"/>
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
    </Grid>
</Page>
