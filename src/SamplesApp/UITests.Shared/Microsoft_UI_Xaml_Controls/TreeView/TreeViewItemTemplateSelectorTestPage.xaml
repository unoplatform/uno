<!--  Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information.  -->
<utilities:MUXTestPage x:Class="UITests.Shared.Microsoft_UI_Xaml_Controls.TreeViewTests.TreeViewItemTemplateSelectorTestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Microsoft_UI_Xaml_Controls.TreeViewTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxcontrols="using:Microsoft.UI.Xaml.Controls"
    xmlns:utilities="using:MUXControlsTestApp.Utilities"
    mc:Ignorable="d">

	<Page.Resources>
		<ResourceDictionary>
			<DataTemplate x:Key="Template1">
				<TextBlock x:Name="Template1"
				    AutomationProperties.Name="Template1"
				    Text="Template1" />
			</DataTemplate>

			<DataTemplate x:Key="Template2">
				<TextBlock x:Name="Template2"
				    AutomationProperties.Name="Template2"
				    Text="Template2" />
			</DataTemplate>

			<local:TreeViewItemTemplateSelector x:Key="TreeViewItemTemplateSelector"
			    Template1="{StaticResource Template1}"
			    Template2="{StaticResource Template2}" />

			<Style x:Key="Style1"
			    TargetType="muxcontrols:TreeViewItem">
				<Setter Property="Background" Value="Green" />
				<Setter Property="Height" Value="50" />
			</Style>

			<Style x:Key="Style2"
			    TargetType="muxcontrols:TreeViewItem">
				<Setter Property="Background" Value="Yellow" />
				<Setter Property="Height" Value="60" />
			</Style>

			<local:TreeViewItemStyleSelector x:Key="TreeViewItemStyleSelector"
			    Style1="{StaticResource Style1}"
			    Style2="{StaticResource Style2}" />
		</ResourceDictionary>
	</Page.Resources>

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<muxcontrols:TreeView x:Name="TestTreeView"
		    Width="400"
		    Height="400"
		    HorizontalAlignment="Left"
		    VerticalAlignment="Top"
		    ItemContainerStyleSelector="{StaticResource TreeViewItemStyleSelector}"
		    ItemTemplateSelector="{StaticResource TreeViewItemTemplateSelector}" />
	</Grid>
</utilities:MUXTestPage>
