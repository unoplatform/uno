<!--  Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information.  -->
<local:TestPage
    x:Class="MUXControlsTestApp.BreadcrumbBarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:MUXControlsTestApp"
    xmlns:local2="using:Breadcrumb_TestUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="BreadcrumbTestPage"
    mc:Ignorable="d">

    <Grid Margin="12" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical">
            <controls:BreadcrumbBarItem />

            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition x:Name="BreadcrumbContainerColumn" Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel
                    Grid.Column="0"
                    MinWidth="250"
                    Padding="12"
                    Background="{ThemeResource SystemChromeLowColor}"
                    Orientation="Vertical">
                    <TextBlock Style="{ThemeResource StandardGroupHeader}" Text="BreadcrumbBar control width" />
                    <Slider
                        x:Name="WidthSlider"
                        AutomationProperties.Name="WidthSlider"
                        Maximum="700"
                        Minimum="100"
                        ValueChanged="WidthSlider_ValueChanged"
                        Value="700" />

                    <CheckBox
                        x:Name="RTLCheckbox"
                        AutomationProperties.Name="RightToLeftCheckbox"
                        Checked="RTL_Checked"
                        Content="BreadcrumbBar is RTL"
                        Unchecked="RTL_Unchecked" />

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Last clicked element: " />
                        <TextBlock
                            x:Name="LastClickedItem"
                            AutomationProperties.Name="LastClickedItem"
                            Text="" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Last clicked element index: " />
                        <TextBlock
                            x:Name="LastClickedItemIndex"
                            AutomationProperties.Name="LastClickedItemIndex"
                            Text="-1" />
                    </StackPanel>
                </StackPanel>

                <controls:BreadcrumbBar
                    x:Name="BreadcrumbControl"
                    Grid.Column="1"
                    AutomationProperties.Name="BreadcrumbControl"
                    ItemClicked="BreadcrumbControl_ItemClicked"
                    ItemsSource="{x:Bind breadCrumbList}">
                    <controls:BreadcrumbBar.ItemTemplate>
                        <DataTemplate x:DataType="local2:TreeNode">
                            <controls:BreadcrumbBarItem Content="{Binding}">
                                <controls:BreadcrumbBarItem.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </controls:BreadcrumbBarItem.ContentTemplate>
                            </controls:BreadcrumbBarItem>
                        </DataTemplate>
                    </controls:BreadcrumbBar.ItemTemplate>
                </controls:BreadcrumbBar>

                <Grid Grid.Column="2" Background="Gray" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <ScrollViewer
                Grid.Row="1"
                HorizontalScrollMode="Disabled"
                VerticalScrollMode="Auto">
                <controls:ItemsRepeater
                    x:Name="ChildrenItemsRepeater"
                    ElementPrepared="Child_ElementPrepared"
                    ItemsSource="{x:Bind currentNodeChildrenList}">
                    <controls:ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="local2:TreeNode">
                            <Button Click="ItemsRepeater_ButtonClick" Content="{Binding}">
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </Button.ContentTemplate>
                            </Button>
                        </DataTemplate>
                    </controls:ItemsRepeater.ItemTemplate>
                </controls:ItemsRepeater>
            </ScrollViewer>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="500" />
                </Grid.ColumnDefinitions>

                <controls:BreadcrumbBar
                    x:Name="BreadcrumbControl2"
                    Grid.Column="0"
                    ItemClicked="BreadcrumbControl_ItemClicked"
                    ItemsSource="{x:Bind breadCrumbList}">
                    <controls:BreadcrumbBar.ItemTemplate>
                        <DataTemplate x:DataType="local2:TreeNode">
                            <controls:BreadcrumbBarItem Content="{Binding}">
                                <controls:BreadcrumbBarItem.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" />
                                    </DataTemplate>
                                </controls:BreadcrumbBarItem.ContentTemplate>
                            </controls:BreadcrumbBarItem>
                        </DataTemplate>
                    </controls:BreadcrumbBar.ItemTemplate>
                </controls:BreadcrumbBar>
            </Grid>
        </StackPanel>
    </Grid>
</local:TestPage>
