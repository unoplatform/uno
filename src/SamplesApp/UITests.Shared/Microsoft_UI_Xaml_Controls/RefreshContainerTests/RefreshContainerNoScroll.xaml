<local:TestPage
    x:Class="MUXControlsTestApp.RefreshContainerNoScroll"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MUXControlsTestApp"
    xmlns:muxcontrols="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<muxcontrols:RefreshContainer Grid.Row="1" x:Name="RefreshContainer" RefreshRequested="RefreshContainer_RefreshRequested" Background="Green">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>
			<Button x:Name="RefreshButton" Click="Refresh_Click">Refresh</Button>
			<ScrollViewer>
				<Border Background="Red" Padding="12" Width="300" Height="300">
					<TextBlock Text="Pull down to refresh" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>
				</Border>
			</ScrollViewer>
		</Grid>
	</muxcontrols:RefreshContainer>
</local:TestPage>
