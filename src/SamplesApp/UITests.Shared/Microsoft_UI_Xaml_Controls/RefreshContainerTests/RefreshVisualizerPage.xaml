<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<local:TestPage
    x:Class="MUXControlsTestApp.RefreshVisualizerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MUXControlsTestApp"
    xmlns:muxcontrols="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:contract5Present="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract, 5)"
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Orientation="Vertical" VerticalAlignment="Center"
        Padding="20" contract5Present:Spacing="10">
      <muxcontrols:RefreshVisualizer x:Name="RefreshVisualizer"/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <Button x:Name="RefreshButton" Content="Refresh" Margin="15"/>
      </StackPanel>
      <Slider x:Name="Slider"
            Minimum="0"
            Maximum="100"
            TickFrequency="2"
            TickPlacement="BottomRight" />
      <ComboBox x:Name="OrientationComboBox"/>
      <TextBox x:Name="StateText"/>
      <Button x:Name="InsertIcon" Content="InsertIcon"/>
      <Button x:Name="InsertGrid" Content="InsertGrid"/>
      <Button x:Name="ChangeBackground" Content="ChangeBackground"/>
      <Button x:Name="ChangeForeground" Content="ChangeForeground"/>
      <Slider x:Name="MarginSlider" Minimum="-10" Maximum ="50" TickFrequency="2" TickPlacement="BottomRight"/>
      <Slider x:Name="MySizeSlider" Minimum="2" Maximum ="50" TickFrequency="2" TickPlacement="BottomRight"/>
    </StackPanel>
</local:TestPage>
