<!--  Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information.  -->
<local:TestPage
    x:Class="MUXControlsTestApp.TitleBarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:exp="using:Microsoft.Experimental.UI.Xaml.Controls"
    xmlns:local="using:MUXControlsTestApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="TitleBarTestPage"
    mc:Ignorable="d">

    <Grid Padding="12" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Margin="2"
                    VerticalAlignment="Center"
                    Text="TitleBar OutputDebugString Levels:" />
                <!--<ComboBox
                    x:Name="cmbTitleBarOutputDebugStringLevel"
                    Grid.Column="1"
                    Width="120"
                    Margin="2"
                    HorizontalAlignment="Left"
                    SelectedIndex="0"
                    SelectionChanged="CmbTitleBarOutputDebugStringLevel_SelectionChanged">
                    <ComboBoxItem>None</ComboBoxItem>
                    <ComboBoxItem>Info</ComboBoxItem>
                    <ComboBoxItem>Verbose</ComboBoxItem>
                </ComboBox>-->
            </Grid>

            <TitleBar x:Name="WindowingTitleBar" Title="Windowing TitleBar">
                <TitleBar.IconSource>
                    <SymbolIconSource Symbol="Home" />
                </TitleBar.IconSource>

                <AutoSuggestBox
                    x:Name="AutoSuggestBoxInTitleBar"
                    MinWidth="300"
                    MaxWidth="500"
                    Margin="8"
                    PlaceholderText="Search pictures, videos or places..."
                    QueryIcon="Find" />
                <TitleBar.RightHeader>
                    <PersonPicture
                        Width="30"
                        Height="30"
                        Initials="JD" />
                </TitleBar.RightHeader>
            </TitleBar>

            <Button Click="TitleBarWindowingButton_Click" Content="New Window with Custom TitleBar" />
        </StackPanel>
    </Grid>
</local:TestPage>
