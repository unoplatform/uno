<Page
	x:Class="UITests.Microsoft_UI_Xaml_Controls.WebView2Tests.WebView2_PostMessage_RemoveAdd"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:controls="using:Microsoft.UI.Xaml.Controls"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid RowSpacing="8" Padding="16">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="300" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
			<Button x:Name="RemoveButton" Content="Remove WebView2" Click="OnRemoveClick" />
			<Button x:Name="AddButton" Content="Add WebView2" Click="OnAddClick" IsEnabled="False" />
			<Button x:Name="SendMessageButton" Content="Send PostMessage from JS" Click="OnSendMessageClick" />
		</StackPanel>

		<Border Grid.Row="1" x:Name="WebViewContainer" BorderBrush="Gray" BorderThickness="1">
			<controls:WebView2 x:Name="TestWebView" />
		</Border>

		<TextBlock Grid.Row="2" Text="Messages received from WebView2:" FontWeight="Bold" />

		<ScrollViewer Grid.Row="3" BorderBrush="Gray" BorderThickness="1">
			<TextBlock x:Name="MessageLog" TextWrapping="Wrap" Padding="4" />
		</ScrollViewer>
	</Grid>
</Page>
