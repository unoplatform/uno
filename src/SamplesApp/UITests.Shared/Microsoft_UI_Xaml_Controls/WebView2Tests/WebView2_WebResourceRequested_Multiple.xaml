<Page
    x:Class="UITests.Microsoft_UI_Xaml_Controls.WebViewTests.WebView2_WebResourceRequested_Multiple"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITests.Microsoft_UI_Xaml_Controls.WebViewTests"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button x:Name="AddFilterButton" Content="Add wildcard filter (*)"/>
            <Button x:Name="NavigateButton" Content="Navigate test page"/>
            <Button x:Name="ClearButton" Content="Clear log"/>
            <TextBlock x:Name="CountText" VerticalAlignment="Center" Margin="8,0,0,0" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" MaxHeight="200" Margin="0,8,0,0">
            <TextBlock x:Name="LogText" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11"/>
        </ScrollViewer>

        <controls:WebView2 x:Name="WebView" Grid.Row="2" Margin="0,8,0,0"/>
    </Grid>

</Page>
