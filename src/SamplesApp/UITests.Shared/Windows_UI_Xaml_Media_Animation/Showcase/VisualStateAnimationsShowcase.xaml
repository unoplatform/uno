<Page
    x:Class="UITests.Windows_UI_Xaml_Media_Animation.Showcase.VisualStateAnimationsShowcase"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Media_Animation.Showcase"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="20" Spacing="12">
            <TextBlock
                FontSize="28"
                FontWeight="Bold"
                Text="VisualState Animations Showcase" />
            <TextBlock
                MaxWidth="700"
                HorizontalAlignment="Left"
                FontSize="13"
                Opacity="0.75"
                Text="VisualStateManager.GoToState drives Storyboards declared inside VisualState. The first card uses GoToState manually; the second uses an AdaptiveTrigger that fires when the window crosses 600px wide."
                TextWrapping="Wrap" />

            <TextBlock
                Margin="0,16,0,4"
                FontSize="20"
                FontWeight="SemiBold"
                Text="1. GoToState (manually toggle)" />
            <local:VisualStateBox />

            <TextBlock
                Margin="0,16,0,4"
                FontSize="20"
                FontWeight="SemiBold"
                Text="2. AdaptiveTrigger (resize window)" />
            <TextBlock
                FontSize="13"
                Opacity="0.75"
                Text="Resize the window narrower than 600px to see the layout switch — the storyboard animates the indicator between states."
                TextWrapping="Wrap" />
            <local:AdaptiveStateBox />
        </StackPanel>
    </ScrollViewer>
</Page>
