<UserControl x:Class="GenericApp.Views.Content.UITests.Animations.DoubleAnimationUsingKeyFrames_TranslateX"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Uno.UI.Samples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

	<UserControl.Resources>
		<x:String x:Key="SplineControlPoints">1, 0, 0, 1</x:String>

		<Style x:Key="icn_loading"
		    TargetType="Button">
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Grid>
							<Border x:Name="Square"
							    Width="50"
							    Height="50"
							    HorizontalAlignment="Left"
							    VerticalAlignment="Top"
							    Background="CornflowerBlue">
								<Border.RenderTransform>
									<TranslateTransform />
								</Border.RenderTransform>
							</Border>
							<VisualStateManager.VisualStateGroups>
								<VisualStateGroup x:Name="CommonStates">
									<VisualState x:Name="Pressed">
										<Storyboard>
											<DoubleAnimationUsingKeyFrames Duration="0:0:4"
											    RepeatBehavior="Forever"
											    Storyboard.TargetName="Square"
											    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
												<DoubleAnimationUsingKeyFrames.KeyFrames>
													<DiscreteDoubleKeyFrame KeyTime="0:0:0"
													    Value="0" />
													<LinearDoubleKeyFrame KeyTime="0:0:1"
													    Value="200" />
													<SplineDoubleKeyFrame KeySpline="{StaticResource SplineControlPoints}"
													    KeyTime="0:0:2"
													    Value="100" />
													<EasingDoubleKeyFrame KeyTime="0:0:4"
													    Value="0">
														<EasingDoubleKeyFrame.EasingFunction>
															<CubicEase EasingMode="EaseInOut" />
														</EasingDoubleKeyFrame.EasingFunction>
													</EasingDoubleKeyFrame>
												</DoubleAnimationUsingKeyFrames.KeyFrames>
											</DoubleAnimationUsingKeyFrames>
										</Storyboard>
									</VisualState>
								</VisualStateGroup>
							</VisualStateManager.VisualStateGroups>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<controls:SampleControl SampleDescription="You should see a blue square moving from left to right (looping) (Tap it to start)">
		<controls:SampleControl.SampleContent>
			<DataTemplate>
				<StackPanel Width="200"
				    Height="50"
				    Background="LightGreen">
					<Button x:Name="Test"
					    Width="50"
					    Height="50"
					    HorizontalAlignment="Left"
					    VerticalAlignment="Top"
					    Style="{StaticResource icn_loading}" />
				</StackPanel>
			</DataTemplate>
		</controls:SampleControl.SampleContent>
	</controls:SampleControl>
</UserControl>
