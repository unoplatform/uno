<Page
    x:Class="UITests.Windows_UI_Core.SystemNavigationManagerTests.HardwareBackButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITests.Windows_UI_Core.SystemNavigationManagerTests"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<StackPanel Spacing="8" Padding="16">
		<TextBlock Text="Android 15+ Behavior" FontWeight="Bold" />
		<TextBlock TextWrapping="Wrap" Text="On Android 15+, subscription state determines back handling. The 'Handle event' checkbox is ignored. Subscribe/unsubscribe controls whether back is consumed." />

		<StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
			<Button Click="Subscribe" Content="Subscribe" x:Name="SubscribeButton" />
			<Button Click="Unsubscribe" Content="Unsubscribe" x:Name="UnsubscribeButton" />
		</StackPanel>
		<TextBlock x:Name="SubscriptionStatusText" Text="Status: Subscribed (via Loaded)" />

		<TextBlock Text="Legacy AppViewBackButtonVisibility" FontWeight="Bold" Margin="0,16,0,0" />
		<StackPanel Orientation="Horizontal" Spacing="8">
			<Button Tapped="Enable" Content="Enable" />
			<Button Tapped="Disable" Content="Disable" />
		</StackPanel>

		<TextBlock Text="Legacy Handled Property (ignored on Android 15+)" FontWeight="Bold" Margin="0,16,0,0" />
		<CheckBox IsThreeState="False" IsChecked="False" Content="Handle event" x:Name="HandleCheckBox" />

		<TextBlock Text="Output:" FontWeight="Bold" Margin="0,16,0,0" />
		<TextBlock x:Name="OutputTextBlock" />
	</StackPanel>
</Page>
