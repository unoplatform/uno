<Page x:Class="UITests.Windows_UI_Xaml_Controls.TextBox.TextBox_Wrapping"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<Grid ColumnSpacing="8">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"
			    MaxWidth="200" />
			<ColumnDefinition Width="*"
			    MaxWidth="200" />
			<ColumnDefinition Width="*"
			    MaxWidth="200" />
			<ColumnDefinition Width="*"
			    MaxWidth="200" />
		</Grid.ColumnDefinitions>

		<StackPanel Grid.Column="0"
		    Spacing="6">
			<TextBlock FontSize="15">
				TextWrapping="Wrap"<LineBreak />
				AcceptsReturn="True"</TextBlock>

			<Border BorderBrush="Orange"
			    BorderThickness="3">
				<TextBox x:Name="wrapReturns"
				    MaxHeight="400"
				    xml:space="preserve"
				    AcceptsReturn="True"
				    Text="{x:Bind Text, Mode=TwoWay}"
				    TextWrapping="Wrap" />
			</Border>
		</StackPanel>

		<StackPanel Grid.Column="1"
		    Spacing="6">
			<TextBlock FontSize="15">
				TextWrapping="NoWrap"<LineBreak />
				AcceptsReturn="False"</TextBlock>

			<Border BorderBrush="Orange"
			    BorderThickness="3">
				<TextBox x:Name="noWrapNoReturns"
				    MaxHeight="400"
				    AcceptsReturn="False"
				    Text="{x:Bind Text, Mode=TwoWay}"
				    TextWrapping="NoWrap" />
			</Border>
		</StackPanel>

		<StackPanel Grid.Column="2"
		    Spacing="6">
			<TextBlock FontSize="15">
				TextWrapping="NoWrap"<LineBreak />
				AcceptsReturn="True"</TextBlock>

			<Border BorderBrush="Orange"
			    BorderThickness="3">
				<TextBox x:Name="NoWrapReturns"
				    MaxHeight="400"
				    AcceptsReturn="True"
				    Text="{x:Bind Text, Mode=TwoWay}"
				    TextWrapping="NoWrap" />
			</Border>
		</StackPanel>

		<StackPanel Grid.Column="3"
		    Spacing="6">
			<TextBlock FontSize="15">
				TextWrapping="Wrap"<LineBreak />
				AcceptsReturn="False"</TextBlock>

			<Border BorderBrush="Orange"
			    BorderThickness="3">
				<TextBox x:Name="wrapNoReturns"
				    MaxHeight="400"
				    AcceptsReturn="False"
				    Text="{x:Bind Text, Mode=TwoWay}"
				    TextWrapping="Wrap" />
			</Border>
		</StackPanel>

		<TextBlock Grid.ColumnSpan="3"
		    VerticalAlignment="Bottom">
			TextSource:<Hyperlink NavigateUri="https://vlad-saling.github.io/star-trek-ipsum/">https://vlad-saling.github.io/star-trek-ipsum/</Hyperlink>
		</TextBlock>
	</Grid>
</Page>
