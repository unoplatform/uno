<Page x:Class="UITests.Windows_UI_Xaml_Controls.Repeater.ItemsRepeaterManyItems"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Controls.Repeater"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mux="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<Grid>
		<ScrollViewer>
			<mux:ItemsRepeater x:Name="itemsRepeater"
			    Margin="4">
				<mux:ItemsRepeater.Layout>
					<mux:UniformGridLayout ItemsJustification="Start"
					    MinColumnSpacing="4"
					    MinItemHeight="100"
					    MinItemWidth="100"
					    MinRowSpacing="4" />
				</mux:ItemsRepeater.Layout>
				<mux:ItemsRepeater.ItemTemplate>
					<DataTemplate>
						<Border Background="LightGray">
							<TextBlock HorizontalAlignment="Center"
							    VerticalAlignment="Center"
							    Text="{Binding}" />
						</Border>
					</DataTemplate>
				</mux:ItemsRepeater.ItemTemplate>
			</mux:ItemsRepeater>
		</ScrollViewer>
	</Grid>
</Page>
