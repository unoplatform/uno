<UserControl
	x:Class="Uno.UI.Samples.Content.UITests.GridView.GridView_Vertical_MaxItemWidth" 
	xmlns:controls="using:Uno.UI.Samples.Controls"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="Uno.UI.Samples.Content.UITests.GridView"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:u="using:nVentive.Umbrella.Views.Controls"
	xmlns:uBehaviors="using:nVentive.Umbrella.Views.Behaviors"
	xmlns:ios="http://uno.ui/ios"
	xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:android="http://uno.ui/android"
	mc:Ignorable="d ios android"
	d:DesignHeight="2000"
	d:DesignWidth="400">

	<controls:SampleControl SampleDescription="Demonstrates the use of the ItemMaxWidth property on supported platforms, where the items should use up all the space.">
		<controls:SampleControl.SampleContent>
			<DataTemplate>
				<StackPanel>
					<Border BorderThickness="1" BorderBrush="Green" Height="150" Width="300">
						<GridView ItemsSource="{Binding [SampleItems]}"
									win:ScrollViewer.HorizontalScrollBarVisibility="Auto"
									win:ScrollViewer.HorizontalScrollMode="Auto"
									>
							<GridView.ItemTemplate>
								<DataTemplate>
									<Border Background="White">
										<TextBlock Text="{Binding}" Foreground="Blue" Height="50" />
									</Border>
								</DataTemplate>
							</GridView.ItemTemplate>
						</GridView>
					</Border>
					<Border BorderThickness="1" BorderBrush="Purple" Height="150" Width="400">
						<GridView ItemsSource="{Binding [SampleItems]}"
									win:ScrollViewer.HorizontalScrollBarVisibility="Auto"
									win:ScrollViewer.HorizontalScrollMode="Auto"
									>
							<GridView.ItemTemplate>
								<DataTemplate>
									<Border Background="White">
										<TextBlock Text="{Binding}" Foreground="Blue" Height="50" />
									</Border>
								</DataTemplate>
							</GridView.ItemTemplate>
						</GridView>
					</Border>
					<Border BorderThickness="1" BorderBrush="Purple" Height="150" Width="500">
						<GridView ItemsSource="{Binding [SampleItems]}"
									win:ScrollViewer.HorizontalScrollBarVisibility="Auto"
									win:ScrollViewer.HorizontalScrollMode="Auto"
									>
							<GridView.ItemTemplate>
								<DataTemplate>
									<Border Background="White">
										<TextBlock Text="{Binding}" Foreground="Blue" Height="50" />
									</Border>
								</DataTemplate>
							</GridView.ItemTemplate>
						</GridView>
					</Border>
				</StackPanel>
			</DataTemplate>
		</controls:SampleControl.SampleContent>
	</controls:SampleControl>
</UserControl>