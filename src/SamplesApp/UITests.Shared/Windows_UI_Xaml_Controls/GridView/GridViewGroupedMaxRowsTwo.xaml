<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.GridView.GridViewGroupedMaxRowsTwo"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.GridView"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:controls="using:Uno.UI.Samples.Controls"
			 mc:Ignorable="">

	<controls:SampleControl SampleDescription="GridViewGroupedMaxRowsTwo">
		<controls:SampleControl.SampleContent>
			<DataTemplate>

				<Border Background="Red">

					<GridView ItemsSource="{Binding [GroupedSampleItemsAsSource]}"
							  ScrollViewer.HorizontalScrollBarVisibility="Auto"
							  ScrollViewer.HorizontalScrollMode="Auto">

						<GridView.ItemsPanel>
							<ItemsPanelTemplate>
								<ItemsWrapGrid  MaximumRowsOrColumns="2" />
							</ItemsPanelTemplate>
						</GridView.ItemsPanel>

						<GridView.GroupStyle>
							<GroupStyle>
								<GroupStyle.HeaderTemplate>
									<DataTemplate>

										<Border Background="Blue">

											<TextBlock Text="{Binding Key}"
													   Foreground="Red"
													   FontSize="32"
													   Height="40"
													   Width="200" />
										</Border>
									</DataTemplate>
								</GroupStyle.HeaderTemplate>
							</GroupStyle>
						</GridView.GroupStyle>
						<GridView.ItemTemplate>
							<DataTemplate>

								<Border Background="White">

									<TextBlock Text="{Binding}"
											   Foreground="Blue"
											   Height="16"
											   Width="100" />
								</Border>
							</DataTemplate>
						</GridView.ItemTemplate>
					</GridView>
				</Border>
			</DataTemplate>
		</controls:SampleControl.SampleContent>
	</controls:SampleControl>
</UserControl>
