<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ScrollViewerTests.Hosted_ScrollViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ScrollViewerTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

	<StackPanel>
		<TextBlock Text="Should show [OuterContentPresenter Tag] 2 times" />

		<ContentControl Margin="0,20"
		    Tag="OuterContentPresenter Tag">
			<ContentControl.Template>
				<ControlTemplate TargetType="ContentControl">
					<Border>
						<ScrollViewer Tag="ScrollViewer Tag">
							<ScrollViewer.Template>
								<ControlTemplate TargetType="ScrollViewer">
									<Border Background="{TemplateBinding Background}"
									    BorderBrush="{TemplateBinding BorderBrush}"
									    BorderThickness="{TemplateBinding BorderThickness}"
									    CornerRadius="{TemplateBinding CornerRadius}">
										<ScrollContentPresenter x:Name="ScrollContentPresenter"
										    Margin="{TemplateBinding Padding}" />
									</Border>
								</ControlTemplate>
							</ScrollViewer.Template>
							<ContentPresenter x:Name="SUT_1"
							    Content="{TemplateBinding Tag}" />
						</ScrollViewer>
					</Border>
				</ControlTemplate>
			</ContentControl.Template>
		</ContentControl>

		<ContentControl Tag="OuterContentPresenter Tag">
			<ContentControl.Template>
				<ControlTemplate TargetType="ContentControl">
					<Border>
						<ScrollViewer Tag="ScrollViewer Tag">
							<ScrollViewer.Template>
								<ControlTemplate TargetType="ScrollViewer">
									<Border Background="{TemplateBinding Background}"
									    BorderBrush="{TemplateBinding BorderBrush}"
									    BorderThickness="{TemplateBinding BorderThickness}"
									    CornerRadius="{TemplateBinding CornerRadius}">
										<ScrollContentPresenter x:Name="ScrollContentPresenter"
										    Margin="{TemplateBinding Padding}"
										    Content="{TemplateBinding Content}" />
									</Border>
								</ControlTemplate>
							</ScrollViewer.Template>
							<ContentPresenter x:Name="SUT_2"
							    Content="{TemplateBinding Tag}" />
						</ScrollViewer>
					</Border>
				</ControlTemplate>
			</ContentControl.Template>
		</ContentControl>
	</StackPanel>
</UserControl>
