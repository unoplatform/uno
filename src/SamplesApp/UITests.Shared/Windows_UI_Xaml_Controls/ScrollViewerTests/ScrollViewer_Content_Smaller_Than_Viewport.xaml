<Page x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ScrollViewerTests.ScrollViewer_Content_Smaller_Than_Viewport"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<Page.Resources>
		<Style x:Key="HorizontalScrollViewerStyle"
		    TargetType="ScrollViewer">
			<Setter Property="HorizontalScrollMode" Value="Auto" />
			<Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
			<Setter Property="VerticalScrollMode" Value="Disabled" />
			<Setter Property="VerticalScrollBarVisibility" Value="Hidden" />
			<Setter Property="Margin" Value="0,10" />
		</Style>
	</Page.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<ScrollViewer Style="{StaticResource HorizontalScrollViewerStyle}">
			<StackPanel Orientation="Horizontal"
			    Spacing="8">
				<TextBlock FontSize="40"
				    Text="Not scrollable." />
			</StackPanel>
		</ScrollViewer>

		<ScrollViewer Grid.Row="1"
		    Style="{StaticResource HorizontalScrollViewerStyle}">
			<StackPanel Orientation="Horizontal"
			    Spacing="8">
				<TextBlock FontSize="40"
				    Text="This line should not be scrollable." />
			</StackPanel>
		</ScrollViewer>

		<ScrollViewer Grid.Row="2"
		    Style="{StaticResource HorizontalScrollViewerStyle}">
			<StackPanel Orientation="Horizontal"
			    Spacing="8">
				<TextBlock FontSize="40"
				    Text="This line might be scrollable horizontally but not vertically." />
			</StackPanel>
		</ScrollViewer>

		<ScrollViewer Grid.Row="3"
		    Style="{StaticResource HorizontalScrollViewerStyle}">
			<StackPanel Orientation="Horizontal"
			    Spacing="8">
				<TextBlock FontSize="40"
				    Text="This extremely long and exceedingly wide line should be scrollable horizontally but not vertically." />
			</StackPanel>
		</ScrollViewer>
	</Grid>
</Page>
