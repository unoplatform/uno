<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ScrollViewerTests.ScrollViewer_Zoom_Inside_FlipView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ScrollViewerTests"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:controls="using:Uno.UI.Samples.Controls"
			 mc:Ignorable="">

	<controls:SampleControl SampleDescription="Zoomable ScrollViewer nested inside FlipView">
		<controls:SampleControl.SampleContent>
			<DataTemplate>

				<FlipView ItemsSource="{Binding [SampleItems]}"
						  x:Name="FlipView"
						  Width="300"
						  Height="150"
						  Background="Red">

					<FlipView.ItemTemplate>
						<DataTemplate>

							<Border BorderBrush="Blue"
									BorderThickness="3">
								<StackPanel>

									<TextBlock Height="60"
											   Foreground="Blue">
										<Run Text="Image: " /><Run Text="{Binding}" />
									</TextBlock>

									<ScrollViewer Width="300"
												  Height="100"
												  HorizontalScrollMode="Enabled"
												  HorizontalScrollBarVisibility="Visible"
												  VerticalScrollMode="Enabled"
												  VerticalScrollBarVisibility="Visible"
												  ZoomMode="Enabled"
												  MinZoomFactor="0.25"
												  MaxZoomFactor="4">

										<Image Source="{Binding}"
											   Width="300"
											   Height="100" />
									</ScrollViewer>
								</StackPanel>
							</Border>
						</DataTemplate>
					</FlipView.ItemTemplate>
				</FlipView>
			</DataTemplate>
		</controls:SampleControl.SampleContent>
	</controls:SampleControl>
</UserControl>
