<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.TimePicker.TimePickerSelector_Simple"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.TimePicker"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:controls="using:Uno.UI.Samples.Controls"
			 xmlns:xamarin="http://umbrella/xamarin"
			 mc:Ignorable="xamarin">

	<controls:SampleControl SampleDescription="TimePickerSelector sample">
		<controls:SampleControl.SampleContent>
			<DataTemplate>
				
				<win:TextBlock Text="TimePickerSelector is a Xamarin-only control (used for native styling)." />

				<xamarin:StackPanel>
					<StackPanel Orientation="Horizontal"
								Background="PowderBlue">
						<TextBlock Text="Selected time: " />
						<TextBlock Text="{Binding [SelectedTime]}" />
					</StackPanel>

					<TimePickerSelector Time="{Binding [SelectedTime], Mode=TwoWay}" />

					<StackPanel Orientation="Horizontal">

						<TextBlock Text="Set hour:" />

						<TextBox Text="{Binding [HourToSet], Mode=TwoWay}"
								 InputScope="Number" />

						<TextBlock Text="Set minute:" />

						<TextBox Text="{Binding [MinuteToSet], Mode=TwoWay}"
								 InputScope="Number" />

						<Button Content="Apply"
								Command="{Binding [SetSelectedTime]}" />
					</StackPanel>
				</xamarin:StackPanel>
			</DataTemplate>
		</controls:SampleControl.SampleContent>
	</controls:SampleControl>
</UserControl>
