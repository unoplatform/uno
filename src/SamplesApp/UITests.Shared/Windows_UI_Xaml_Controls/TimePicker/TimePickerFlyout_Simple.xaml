<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.TimePicker.TimePickerFlyout_Simple"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.TimePicker"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:controls="using:Uno.UI.Samples.Controls"
			 mc:Ignorable="">

	<controls:SampleControl SampleDescription="Description for sample of TimePickerFlyout_Simple">
		<controls:SampleControl.SampleContent>
			<DataTemplate>
				<StackPanel>

					<StackPanel Orientation="Horizontal"
								Background="PowderBlue">
						<TextBlock Text="Selected time: " />
						<TextBlock Text="{Binding [SelectedTime]}" />
					</StackPanel>

					<Button Content="Press here to set time">
						<Button.Flyout>
							<TimePickerFlyout Time="{Binding [SelectedTime], Mode=TwoWay}" />
						</Button.Flyout>
					</Button>

					<StackPanel Orientation="Horizontal">

						<TextBlock Text="Set hour:" />

						<TextBox Text="{Binding [HourToSet], Mode=TwoWay}"
								 InputScope="Number" />

						<TextBlock Text="Set minute:" />

						<TextBox Text="{Binding [MinuteToSet], Mode=TwoWay}"
								 InputScope="Number" />

						<Button Content="Apply"
								Command="{Binding [SetSelectedTime]}" />
					</StackPanel>
				</StackPanel>
			</DataTemplate>
		</controls:SampleControl.SampleContent>
	</controls:SampleControl>
</UserControl>
