<Page x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ComboBox.ComboBox_ItemDataContext"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ComboBox"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<StackPanel>
		<TextBlock>
			Control DataContext:<Run Text="{Binding}" />
			<LineBreak />
			DataContext.Value:<Run Text="{Binding Value}" />
		</TextBlock>

		<ComboBox DisplayMemberPath="Value"
		    ItemsSource="{Binding Items}" />

	</StackPanel>
</Page>
