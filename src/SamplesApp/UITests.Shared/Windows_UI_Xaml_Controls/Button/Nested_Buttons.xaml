<UserControl x:Class="nVentive.Umbrella.Views.UI.Samples.Content.UITests.ButtonTestsControl.Nested_Buttons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Uno.UI.Samples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:not_win="http://uno.ui/not_win"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d not_win">

	<controls:SampleControl SampleDescription="Nested_Buttons">
		<controls:SampleControl.SampleContent>
			<DataTemplate>
				<StackPanel>
					<Button Name="Outer"
					    Width="200"
					    Height="200"
					    Background="Tomato"
					    Command="{Binding [OuterCommand]}">
						<Button.Template>
							<ControlTemplate>
								<Border Background="{TemplateBinding Background}">
									<Button Name="Inner"
									    Width="100"
									    Height="100"
									    HorizontalAlignment="Center"
									    VerticalAlignment="Center"
									    Background="Olive"
									    Command="{Binding [InnerCommand]}">
										<Button.Template>
											<ControlTemplate>
												<Border Background="{TemplateBinding Background}" />
											</ControlTemplate>
										</Button.Template>
									</Button>
								</Border>
							</ControlTemplate>
						</Button.Template>
					</Button>
					<TextBlock Name="ValidateCommandTextBlock"
					    Text="{Binding [Message]}" />
				</StackPanel>
			</DataTemplate>
		</controls:SampleControl.SampleContent>
	</controls:SampleControl>
</UserControl>
