<UserControl
    x:Class="Uno.UI.Samples.Content.UITests.ButtonTestsControl.ComboBox_Simple"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Uno.UI.Samples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:not_win="http://uno.ui/not_win"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d not_win">

    <controls:SampleControl SampleDescription="ComboBox_Simple">
        <controls:SampleControl.SampleContent>
            <DataTemplate>
                <StackPanel>
                    <ComboBox
                        not_win:Command="{Binding [ComboBoxSimple_Command]}"
                        ItemsSource="{Binding [ComboBoxSimple_ItemsSource]}"
                        SelectedItem="{Binding [ComboBoxSimple_SelectedItem]}" />
                    <TextBlock Text="{Binding [ComboBoxSimple_SelectedItem]}" />
                    <Button Command="{Binding [ComboBoxSimple_GenerateNewList]}" Content="Generate new list" />
                </StackPanel>
            </DataTemplate>
        </controls:SampleControl.SampleContent>
    </controls:SampleControl>
</UserControl>
