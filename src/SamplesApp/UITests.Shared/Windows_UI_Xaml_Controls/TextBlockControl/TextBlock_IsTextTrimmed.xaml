<UserControl x:Class="Uno.UI.Samples.Content.UITests.TextBlockControl.TextBlock_IsTextTrimmed"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d"
			 d:DesignHeight="800"
			 d:DesignWidth="800">

	<StackPanel Spacing="20"
				Padding="20">
		<Slider x:Name="WidthSlider"
				Minimum="0"
				Maximum="25"
				Value="0" />
		<TextBlock>
			<Run Text="MaxLines: " />
			<Run Text="{Binding ElementName=WidthSlider, Path=Value}" />
		</TextBlock>

		<StackPanel Background="LightGray"
					Width="80">

			<TextBlock x:Name="LongText"
					   Text="Lorem ipsum dolor sit amet consectetur. Porta habitasse iaculis non egestas posuere in tincidunt. Risus nunc a  urna est nunc ac mattis eget. Faucibus ullamcorper ipsum faucibus in tristique lacus. Justo cursus arcu ut tristique."
					   TextWrapping="Wrap"
					   MaxLines="{Binding ElementName=WidthSlider, Path=Value}"
					   TextTrimming="CharacterEllipsis" />

		</StackPanel>

		<TextBlock x:Name="TrimmedStatus">
			<Run Text="IsTextTrimmed: " />
			<Run x:Name="IsTextTrimmed" Text="{Binding ElementName=LongText, Path=IsTextTrimmed}" />
		</TextBlock>
	</StackPanel>
</UserControl>
