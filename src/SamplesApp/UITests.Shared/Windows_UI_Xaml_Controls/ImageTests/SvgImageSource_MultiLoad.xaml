<Page x:Class="UITests.Windows_UI_Xaml_Controls.ImageTests.SvgImageSource_MultiLoad"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Controls.ImageTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<StackPanel Spacing="4">
			<ComboBox Header="SVG image"
			    ItemsSource="{x:Bind Sources}"
			    SelectedItem="{x:Bind SelectedSource, Mode=TwoWay}">
				<ComboBox.ItemTemplate>
					<DataTemplate x:DataType="local:SampleSvgSource">
						<TextBlock Text="{x:Bind Name}" />
					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
			<CheckBox Content="Reuse image source"
			    IsChecked="{x:Bind ReuseImageSource, Mode=TwoWay}" />
		</StackPanel>

		<Grid x:Name="ImageContainer"
		    Grid.Row="1" />
	</Grid>
</Page>
