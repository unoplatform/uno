<Page
	x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ViewBoxTests.ViewBox_Grid_None"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ViewBoxTests"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<Viewbox Grid.Row="1" Stretch="None">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock>
                        <Run FontSize="120" Text="Test Text" /><Run FontSize="80" Text="SmallerText" />
                    </TextBlock>
                </Grid>
                <StackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button Style="{StaticResource AccentButtonStyle}">
                        <SymbolIcon Symbol="Home" />
                    </Button>
                    <Button Style="{StaticResource AccentButtonStyle}">
                        <SymbolIcon Symbol="Flag" />
                    </Button>
                    <Button Style="{StaticResource AccentButtonStyle}">
                        <SymbolIcon Symbol="Undo" />
                    </Button>
                </StackPanel>
            </Grid>
        </Viewbox>
	</Grid>
</Page>
