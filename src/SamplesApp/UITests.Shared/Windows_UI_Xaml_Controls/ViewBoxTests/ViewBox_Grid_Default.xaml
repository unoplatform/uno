<Page
	x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ViewBoxTests.ViewBox_Grid_Default"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ViewBoxTests"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Viewbox Grid.Row="0">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock>
                    <Run FontSize="18" Text="This is some long text" /><Run FontSize="14" Text="that should not be clipped" />
                </TextBlock>
            </Grid>
        </Viewbox>
        <StackPanel Grid.Row="1" HorizontalAlignment="Right">
            <Button>
                <SymbolIcon Symbol="Play" />
            </Button>
            <Button>
                <SymbolIcon Symbol="Setting" />
            </Button>
        </StackPanel>
    </Grid>
</Page>
