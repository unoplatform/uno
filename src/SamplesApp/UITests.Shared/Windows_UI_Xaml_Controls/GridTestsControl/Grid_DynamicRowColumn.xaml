<Page
    x:Class="UITests.Windows_UI_Xaml_Controls.GridTestsControl.Grid_DynamicRowColumn"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <StackPanel Spacing="4">
        <StackPanel Orientation="Horizontal">
            <TextBlock Width="120">Column Widths</TextBlock>
            <Slider
                x:Name="width"
                Width="200"
                Maximum="100"
                Minimum="0"
                Value="20" />
            <TextBlock x:Name="widths" Text="{Binding Value, ElementName=width}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <TextBlock Width="120">Column Heights</TextBlock>
            <Slider
                x:Name="height"
                Width="200"
                Maximum="100"
                Minimum="0"
                Value="20" />
            <TextBlock x:Name="heights" Text="{Binding Value, ElementName=height}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <TextBlock Width="120">Column</TextBlock>
            <Slider
                x:Name="col"
                Width="200"
                Maximum="4"
                Minimum="0"
                Value="2" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <TextBlock Width="120">Row</TextBlock>
            <Slider
                x:Name="row"
                Width="200"
                Maximum="4"
                Minimum="0"
                Value="2" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <TextBlock Width="120">Column Span</TextBlock>
            <Slider
                x:Name="colspan"
                Width="200"
                Maximum="4"
                Minimum="1"
                Value="2" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <TextBlock Width="120">Row Span</TextBlock>
            <Slider
                x:Name="rowspan"
                Width="200"
                Maximum="4"
                Minimum="1"
                Value="2" />
        </StackPanel>

        <Grid
            x:Name="sut"
            Margin="20"
            HorizontalAlignment="Left"
            Background="Blue"
            ColumnSpacing="5"
            RowSpacing="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding Text, ElementName=widths}" />
                <ColumnDefinition Width="{Binding Text, ElementName=widths}" />
                <ColumnDefinition Width="{Binding Text, ElementName=widths}" />
                <ColumnDefinition Width="{Binding Text, ElementName=widths}" />
                <ColumnDefinition Width="{Binding Text, ElementName=widths}" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="{Binding Text, ElementName=heights}" />
                <RowDefinition Height="{Binding Text, ElementName=heights}" />
                <RowDefinition Height="{Binding Text, ElementName=heights}" />
                <RowDefinition Height="{Binding Text, ElementName=heights}" />
                <RowDefinition Height="{Binding Text, ElementName=heights}" />
            </Grid.RowDefinitions>

            <Rectangle
                Grid.Row="{Binding Value, ElementName=row}"
                Grid.RowSpan="{Binding Value, ElementName=rowspan}"
                Grid.Column="{Binding Value, ElementName=col}"
                Grid.ColumnSpan="{Binding Value, ElementName=colspan}"
                Fill="Red" />
        </Grid>
    </StackPanel>
</Page>
