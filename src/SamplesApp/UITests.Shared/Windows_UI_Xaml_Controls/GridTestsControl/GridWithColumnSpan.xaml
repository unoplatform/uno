<UserControl x:Class="Uno.UI.Samples.Content.UITests.GridTestsControl.GridWithColumnSpan"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:android="http://uno.ui/android"
    xmlns:controls="using:Uno.UI.Samples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ios="http://uno.ui/ios"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    d:DesignHeight="400"
    d:DesignWidth="500"
    mc:Ignorable="d ios android">

	<controls:SampleControl SampleDescription="The textblock in this test should span on two columns">
		<controls:SampleControl.SampleContent>
			<DataTemplate>
				<Grid Background="LightBlue">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="200" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<TextBlock Grid.ColumnSpan="2"
					    HorizontalAlignment="Left"
					    Text="This is a very long text that should span on the two columns of this grid. That means its size should not be limited by anything but the whole grid itself." />

					<Border Grid.Row="1"
					    Height="200"
					    HorizontalAlignment="Stretch"
					    VerticalAlignment="Top"
					    Background="LightGreen" />

					<Border Grid.Row="1"
					    Grid.Column="1"
					    Height="200"
					    HorizontalAlignment="Stretch"
					    VerticalAlignment="Top"
					    Background="LightCoral" />
				</Grid>
			</DataTemplate>
		</controls:SampleControl.SampleContent>
	</controls:SampleControl>
</UserControl>
