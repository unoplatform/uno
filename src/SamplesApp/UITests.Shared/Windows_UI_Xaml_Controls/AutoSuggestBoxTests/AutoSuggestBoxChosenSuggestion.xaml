<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.AutoSuggestBoxTests.AutoSuggestBoxChosenSuggestion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:UITests.Shared.Windows_UI_Xaml_Controls.AutoSuggestBoxTests"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>


		<AutoSuggestBox x:Name="autoSuggestBox"
		    VerticalAlignment="Top"
		    QuerySubmitted="autoSuggestBox_QuerySubmitted"
		    Text="{Binding book.Author}"
		    TextChanged="autoSuggestBox_TextChanged" />
		<TextBlock x:Name="localTextBlock"
		    Grid.Row="1"
		    FontSize="12" />
		<TextBlock x:Name="logger"
		    Grid.Row="3"
		    FontSize="12" />
	</Grid>
</UserControl>
