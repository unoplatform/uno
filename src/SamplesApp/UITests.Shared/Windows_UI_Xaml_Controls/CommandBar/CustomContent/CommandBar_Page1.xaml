<Page x:Class="UITests.Windows_UI_Xaml_Controls.CommandBar.CustomContent.CommandBar_Page1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:not_win="http://uno.ui/not_win"
    xmlns:u="using:Uno.UI.Controls"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d not_win">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<CommandBar x:Name="Page1CommandBar">
			<CommandBar.Content>
				<Border Height="44"
				    HorizontalAlignment="Center"
				    VerticalAlignment="Center">
					<TextBlock Foreground="Blue"
					    Text="Header with custom Content"
					    TextTrimming="CharacterEllipsis"
					    TextWrapping="NoWrap" />
				</Border>
			</CommandBar.Content>
		</CommandBar>

		<StackPanel Grid.Row="1"
		    VerticalAlignment="Center"
		    Spacing="10">

			<TextBlock HorizontalAlignment="Center"
			    Text="Main Page" />

			<TextBlock x:Name="ExpectedSize"
			    HorizontalAlignment="Center"
			    Text="-1" />

			<TextBlock x:Name="CurrentSize"
			    HorizontalAlignment="Center"
			    Text="-1" />

			<TextBlock x:Name="Result"
			    HorizontalAlignment="Center"
			    Text="-1" />
		</StackPanel>

	</Grid>
</Page>
