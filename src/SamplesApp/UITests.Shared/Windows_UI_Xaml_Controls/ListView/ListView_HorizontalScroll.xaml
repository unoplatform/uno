<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ListView.ListView_HorizontalScroll"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ListView"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d"
			 d:DesignHeight="300"
			 d:DesignWidth="400">
	<StackPanel HorizontalAlignment="Center"
				VerticalAlignment="Center">
		<TextBlock AutomationProperties.AutomationId="HelloTextBlock"
				   Text="Hello Uno Platform!"
				   Margin="0,0,0,50"
				   HorizontalAlignment="Center" />
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<ListView x:Name="SampleListView"
					  MaxHeight="150"
					  MaxWidth="700"
					  Grid.Row="0"
					  BorderBrush="Gray"
					  BorderThickness="1"
					  ItemsSource="{x:Bind ViewModel.Items}"
					  ScrollViewer.VerticalScrollBarVisibility="Visible"
					  ScrollViewer.VerticalScrollMode="Enabled"
					  ScrollViewer.HorizontalScrollBarVisibility="Visible"
					  ScrollViewer.HorizontalScrollMode="Enabled"
					  Margin="10">
				<ListView.ItemTemplate>
					<DataTemplate>
						<Grid ColumnSpacing="20">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="100" />
								<ColumnDefinition Width="200" />
								<ColumnDefinition Width="200" />
								<ColumnDefinition Width="500" />
								<ColumnDefinition Width="200" />
							</Grid.ColumnDefinitions>
							<SplitButton Grid.Column="0"
										 Width="85"
										 Content="0"
										 VerticalAlignment="Bottom">
								<SplitButton.Flyout>
									<MenuFlyout Placement="Auto">
										<MenuFlyoutItem Text="Remove"
														Background="Green" />
									</MenuFlyout>
								</SplitButton.Flyout>
							</SplitButton>
							<TextBox Text="{Binding Name}"
									 Grid.Column="1" />
							<TextBox Text="{Binding Code}"
									 Grid.Column="2" />
							<TextBox Text="{Binding Offset}"
									 Grid.Column="3" />
							<TextBox Text="{Binding ItemDate}"
									 Grid.Column="4" />
						</Grid>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</Grid>
	</StackPanel>
</UserControl>
