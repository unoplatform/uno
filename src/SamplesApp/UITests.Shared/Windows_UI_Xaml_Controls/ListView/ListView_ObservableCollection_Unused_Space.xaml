<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ListView.ListView_ObservableCollection_Unused_Space"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ListView"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d"
			 d:DesignHeight="300"
			 d:DesignWidth="400">

	<Grid Background="Beige"
		  Padding="50">
		<StackPanel VerticalAlignment="Top">
			<ListView x:Name="TargetListView"
					  ItemsSource="{x:Bind _vm.MyItems, Mode=OneWay}" />
		</StackPanel>
		<StackPanel VerticalAlignment="Bottom">
			<Button x:Name="AddItemsButton"
					Content="Add items"
					Click="AddItems" />
			<TextBlock x:Name="StatusTextBlock"
					   Text="Not started" />
			<TextBlock x:Name="HeightTextBlock"
					   Text="Unset" />
		</StackPanel>
	</Grid>
</UserControl>
