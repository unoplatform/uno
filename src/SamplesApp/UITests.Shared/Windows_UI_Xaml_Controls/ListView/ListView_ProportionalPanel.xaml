<UserControl x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ListView.ListView_ProportionalPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:u="using:UITests.Shared.Windows_UI_Xaml_Controls.ListView"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

	<UserControl.Resources>
		<DataTemplate x:Key="ProportionalItemTemplate">
			<u:ProportionalPanel Margin="10"
			    Mode="HeightFollowsWidth"
			    Ratio="1">
				<Grid HorizontalAlignment="Stretch"
				    VerticalAlignment="Stretch">
					<Grid.RowDefinitions>
						<RowDefinition Height="*" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<Grid Grid.Row="1"
					    Height="55"
					    Margin="0,-5,0,0"
					    HorizontalAlignment="Stretch"
					    BorderBrush="Pink"
					    BorderThickness="2"
					    CornerRadius="0,0,5,5">

						<TextBlock HorizontalAlignment="Center"
						    VerticalAlignment="Center"
						    Text="Read more" />
					</Grid>

					<Border Background="SkyBlue"
					    CornerRadius="5,5,0,0">
						<ContentControl Padding="5,0"
						    HorizontalAlignment="Stretch"
						    VerticalAlignment="Stretch"
						    HorizontalContentAlignment="Stretch"
						    VerticalContentAlignment="Stretch"
						    Content="{Binding}" />
					</Border>
				</Grid>
			</u:ProportionalPanel>
		</DataTemplate>
	</UserControl.Resources>

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<ListView ItemTemplate="{StaticResource ProportionalItemTemplate}"
		    ItemsSource="0123456789" />
	</Grid>
</UserControl>
