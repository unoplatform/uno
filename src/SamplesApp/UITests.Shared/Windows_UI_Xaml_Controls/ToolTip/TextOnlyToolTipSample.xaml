<Page x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ToolTip.TextOnlyToolTipSample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ToolTip"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

	<StackPanel>
		<TextBlock FontSize="20"
		    FontWeight="Bold">
			Just a ToolTip control...
		</TextBlock>
		<ToolTip x:Name="inlineTooltip">This is just a boring tooltip control directly in the Visual Tree...</ToolTip>

		<TextBlock FontSize="20"
		    FontWeight="Bold">
			Text only tooltip on a rectangle
		</TextBlock>
		<Rectangle x:Name="rect1"
		    Width="150"
		    Height="70"
		    Fill="Red"
		    ToolTipService.ToolTip="THIS IS THE TOOLTIP TEXT CONTENT" />

		<TextBlock FontSize="20"
		    FontWeight="Bold">
			Complex tooltip on a rectangle displaying visual tree path to root
		</TextBlock>
		<Rectangle x:Name="rect2"
		    Width="150"
		    Height="70"
		    Fill="Blue">
			<ToolTipService.ToolTip>
				<ToolTip x:Name="richToolTip">
					<local:ShowPathToRootControl />
				</ToolTip>
			</ToolTipService.ToolTip>
		</Rectangle>

	</StackPanel>
</Page>
