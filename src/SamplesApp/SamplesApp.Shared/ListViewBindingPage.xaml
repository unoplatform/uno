<Page
    x:Class="SamplesApp.ListViewBindingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SamplesApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="CurrentPage"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

		<ListView
                    Margin="0,20,0,0" 
                    HorizontalContentAlignment="Stretch"
                    SelectionMode="None"
                    IsItemClickEnabled="False">
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="HorizontalContentAlignment" Value="Stretch" />
					<Setter Property="Padding" Value="0" />
					<Setter Property="Margin" Value="0" />
				</Style>
			</ListView.ItemContainerStyle>
			<ListView.ItemTemplate>
				<DataTemplate>
					<RelativePanel Margin="0,20,0,0">
						<Button Content="{Binding }" Command="{Binding DataContext.TestCommand,ElementName=CurrentPage}"  FontSize="12" />
					</RelativePanel>

				</DataTemplate>
			</ListView.ItemTemplate>

			<x:String>test1</x:String>
			<x:String>test2</x:String>
		</ListView>
	</Grid>
</Page>
