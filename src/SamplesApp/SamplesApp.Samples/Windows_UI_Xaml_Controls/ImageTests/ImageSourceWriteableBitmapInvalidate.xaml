<Page
    x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ImageTests.ImageSourceWriteableBitmapInvalidate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ImageTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <StackPanel>
        <Button
            x:Name="_update"
            Click="UpdateSource"
            Content="Update writeable image source" />

        <TextBlock Text="When you click on the button, the writable bitmap (set as source of the image in the orange square bellow) is updated and invalidated. This test makes sure that it appears properly without updating the source on the Image control itself." TextWrapping="Wrap" />

        <Border
            Width="206"
            Height="206"
            BorderBrush="Orange"
            BorderThickness="3">
            <Image x:Name="_image" />
        </Border>
    </StackPanel>
</Page>
