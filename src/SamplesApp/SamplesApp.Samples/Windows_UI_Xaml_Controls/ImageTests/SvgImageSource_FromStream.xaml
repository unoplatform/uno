<Page
    x:Class="UITests.Windows_UI_Xaml_Controls.ImageTests.SvgImageSource_FromStream"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Controls.ImageTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <ComboBox
                Header="SVG image"
                ItemsSource="{x:Bind Sources}"
                SelectedItem="{x:Bind SelectedSource, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="local:SampleSvgSource">
                        <TextBlock Text="{x:Bind Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ComboBox
                Header="Stretch"
                ItemsSource="{x:Bind Stretches}"
                SelectedItem="{x:Bind SelectedStretch, Mode=TwoWay}" />
            <TextBox Header="Image width" Text="{x:Bind ImageWidth, Mode=TwoWay}" />
            <TextBox Header="Image height" Text="{x:Bind ImageHeight, Mode=TwoWay}" />
            <TextBox Header="Rasterized width" Text="{x:Bind RasterizedWidth, Mode=TwoWay}" />
            <TextBox Header="Rasterized height" Text="{x:Bind RasterizedHeight, Mode=TwoWay}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Border
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderBrush="Black"
                BorderThickness="2">
                <Image x:Name="ImageElement" />
            </Border>
        </Grid>
    </Grid>
</Page>
