<Page
    x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ComboBox.ComboBox_MaxDropdownHeight"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ComboBox"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <StackPanel>
        <Slider
            Maximum="1500"
            Minimum="50"
            Value="{Binding MaxDropDownHeight, ElementName=drop1, Mode=TwoWay}" />

        <TextBlock>
            MaxDropDownHeight:<Run Text="{Binding MaxDropDownHeight, ElementName=drop1}" />
        </TextBlock>

        <ComboBox x:Name="drop1" ItemsSource="{Binding VariableLengthItems}" />

    </StackPanel>
</Page>
