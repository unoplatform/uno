<UserControl
    x:Class="UITests.Shared.Windows_UI_Xaml_Controls.Popup.Popup_Overlay_On"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.Popup"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <StackPanel
        x:Name="sampleContent"
        Margin="20,80,20,20"
        Background="Blue">
        <Rectangle
            x:Name="LocatorRectangle"
            Width="50"
            Height="50"
            Fill="Transparent" />
        <CheckBox x:Name="PopupCheckBox" IsChecked="False" />
        <Popup
            IsLightDismissEnabled="True"
            IsOpen="{Binding ElementName=PopupCheckBox, Path=IsChecked, Mode=TwoWay}"
            LightDismissOverlayMode="On">
            <Popup.Child>
                <Border
                    x:Name="PopupChild"
                    Width="75"
                    Height="150"
                    Background="LightPink"
                    CornerRadius="5">
                    <TextBlock Text="YePopupChild" />
                </Border>
            </Popup.Child>
        </Popup>
    </StackPanel>
</UserControl>
