<Page
    x:Class="Uno.UI.Samples.Content.UITests.CommandBar.BackGesture.BackGesture_NavigationCommand"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:Uno.UI.Toolkit"
    mc:Ignorable="d">

    <Grid Background="Orange">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Content="NavigationCommand" Foreground="DarkGray">
            <toolkit:CommandBarExtensions.NavigationCommand>
                <AppBarButton Command="{Binding Command}" Label="MyLabel">
                    <AppBarButton.Icon>
                        <BitmapIcon UriSource="ms-appx:///Assets/Icons/menu.png" />
                    </AppBarButton.Icon>
                </AppBarButton>
            </toolkit:CommandBarExtensions.NavigationCommand>
        </CommandBar>

        <TextBlock
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="Here the CommandBar is visible and has a navigation command. The back gesture SHOULD NOT work."
            TextWrapping="Wrap" />

        <Button
            x:Name="GoBack"
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Click="GoBack_Click"
            Content="Go back" />

    </Grid>
</Page>
