<Page
    x:Class="Uno.UI.Samples.Content.UITests.RichEditBoxControl.RichEditBox_ParagraphFormatting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <StackPanel
        MaxWidth="520"
        Padding="16"
        HorizontalAlignment="Left"
        Spacing="8">
        <TextBlock
            FontWeight="SemiBold"
            Text="RichEditBox paragraph-formatting parity probe: apply Left, Center, Right, or Justify alignment through the Text Object Model and compare rendering and undo behavior across targets."
            TextWrapping="Wrap" />

        <RichEditBox
            x:Name="Editor"
            Width="360"
            Height="160"
            HorizontalAlignment="Left"
            Header="Document" />

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button
                x:Name="LeftButton"
                Click="OnLeftClick"
                Content="Left" />
            <Button
                x:Name="CenterButton"
                Click="OnCenterClick"
                Content="Center" />
            <Button
                x:Name="RightButton"
                Click="OnRightClick"
                Content="Right" />
            <Button
                x:Name="JustifyButton"
                Click="OnJustifyClick"
                Content="Justify" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button
                x:Name="UndoButton"
                Click="OnUndoClick"
                Content="Undo" />
            <Button
                x:Name="RedoButton"
                Click="OnRedoClick"
                Content="Redo" />
            <Button
                x:Name="ResetButton"
                Click="OnResetClick"
                Content="Reset" />
        </StackPanel>

        <TextBlock
            x:Name="Output"
            Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
            TextWrapping="Wrap" />
    </StackPanel>
</Page>
