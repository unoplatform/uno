<UserControl
    x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ListView.ListView_ObservableCollection_Unused_Space"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ListView"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid Padding="50" Background="Beige">
        <StackPanel VerticalAlignment="Top">
            <ListView x:Name="TargetListView" ItemsSource="{x:Bind _vm.MyItems, Mode=OneWay}" />
        </StackPanel>
        <StackPanel VerticalAlignment="Bottom">
            <Button
                x:Name="AddItemsButton"
                Click="AddItems"
                Content="Add items" />
            <TextBlock x:Name="StatusTextBlock" Text="Not started" />
            <TextBlock x:Name="HeightTextBlock" Text="Unset" />
        </StackPanel>
    </Grid>
</UserControl>
