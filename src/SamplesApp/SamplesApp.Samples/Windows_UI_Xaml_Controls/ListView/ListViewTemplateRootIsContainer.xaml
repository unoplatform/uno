<UserControl
    x:Class="UITests.Windows_UI_Xaml_Controls.ListView.ListViewTemplateRootIsContainer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Controls.ListView"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid>
        <ListView
            x:Name="TargetListView"
            Height="200"
            ItemsSource="0123456789ABCDEFGH"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ListViewItem>
                        <ListViewItem.Template>
                            <ControlTemplate TargetType="ListViewItem">
                                <Grid x:Name="ContentBorder">
                                    <Grid
                                        x:Name="ContentPresenterGrid"
                                        BorderBrush="Silver"
                                        BorderThickness="2"
                                        CornerRadius="3">
                                        <TextBlock Text="{Binding}" />
                                    </Grid>
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />
                                            <VisualState x:Name="Selected">
                                                <VisualState.Setters>
                                                    <Setter Target="ContentPresenterGrid.BorderBrush" Value="HotPink" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="PointerOverSelected">
                                                <VisualState.Setters>
                                                    <Setter Target="ContentPresenterGrid.BorderBrush" Value="HotPink" />
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Grid>
                            </ControlTemplate>
                        </ListViewItem.Template>
                    </ListViewItem>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
