<Page
    x:Class="UITests.Windows_UI_Xaml_Input.Keyboard.Keyboard_iOS_Theme"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Input.Keyboard"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel>
            <TextBlock Text="Requested Theme:" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <RadioButton
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    Click="UpdateTheme"
                    Content="Default" />
                <RadioButton
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    Click="UpdateTheme"
                    Content="Light" />
                <RadioButton
                    Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    Click="UpdateTheme"
                    Content="Dark" />
            </Grid>

            <StackPanel x:Name="TestPanel">
                <TextBlock Margin="0,20,0,0" Text="TextBox" />
                <TextBox PlaceholderText="default" />

                <TextBlock Margin="0,20,0,0" Text="PasswordBox" />
                <PasswordBox PlaceholderText="default" />
            </StackPanel>

            <TextBlock TextWrapping="WrapWholeWords">
                <Run>The 'device theme' can be changed by (ios)Settings &gt; Developer &gt; Dark Appearance.</Run><LineBreak /><LineBreak />
                <Run>The keyboard appearance should follow the requested theme, falling back to the os theme.</Run><LineBreak />
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</Page>
