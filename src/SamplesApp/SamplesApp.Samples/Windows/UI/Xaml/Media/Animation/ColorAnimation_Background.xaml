<UserControl
    x:Class="UITests.Windows_UI_Xaml_Media_Animation.ColorAnimation_Background"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Media_Animation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <UserControl.Resources>
        <SolidColorBrush x:Key="ColorAnimation_Background_Brush" Color="Blue" />
        <Storyboard x:Name="colorStoryboard">
            <ColorAnimation
                Storyboard.TargetName="TargetBorder"
                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                To="Red"
                Duration="0:00:0.1" />
        </Storyboard>
    </UserControl.Resources>

    <StackPanel>
        <Button
            x:Name="PlayColorAnimation"
            Click="PlayColorAnimation_Click"
            Content="Play ColorAnimation" />
        <Button
            x:Name="BrushEqualityButton"
            Click="CheckBrushEquality_Click"
            Content="Are brushes ref-equal?" />
        <TextBlock x:Name="BrushEqualityText" Text="-" />
        <TextBlock Text="Status" />
        <TextBlock x:Name="StatusText" />
        <Border
            x:Name="TargetBorder"
            Width="300"
            Height="100"
            Margin="5"
            Background="{StaticResource ColorAnimation_Background_Brush}">
            <Border.RenderTransform>
                <TranslateTransform />
            </Border.RenderTransform>
        </Border>
        <Border
            x:Name="IndependentBorder"
            Width="300"
            Height="100"
            Margin="5"
            Background="{StaticResource ColorAnimation_Background_Brush}" />
    </StackPanel>
</UserControl>
