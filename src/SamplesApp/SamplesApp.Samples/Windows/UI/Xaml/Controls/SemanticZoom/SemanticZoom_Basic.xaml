<UserControl
    x:Class="UITests.Shared.Windows_UI_Xaml_Controls.SemanticZoomTests.SemanticZoom_Basic"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.SemanticZoomTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <CollectionViewSource x:Name="GroupedItems" IsSourceGrouped="True" />

        <DataTemplate x:Key="GroupHeaderTemplate">
            <TextBlock
                Margin="8,16,8,4"
                FontSize="22"
                FontWeight="SemiBold"
                Text="{Binding Key}" />
        </DataTemplate>

        <DataTemplate x:Key="ZoomedOutGroupTemplate">
            <Border
                Width="120"
                Height="80"
                Margin="8"
                Padding="12"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="20"
                    FontWeight="SemiBold"
                    Text="{Binding Group.Key}" />
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid
        Width="720"
        Height="520"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Margin="12"
            Orientation="Horizontal"
            Spacing="12">
            <Button Click="OnToggleView" Content="Toggle view" />
            <TextBlock x:Name="ActiveViewText" VerticalAlignment="Center" />
        </StackPanel>

        <SemanticZoom
            x:Name="SemanticZoomControl"
            Grid.Row="1"
            Margin="12"
            ViewChangeCompleted="OnViewChangeCompleted">
            <SemanticZoom.ZoomedInView>
                <ListView ItemsSource="{Binding Source={StaticResource GroupedItems}}">
                    <ListView.GroupStyle>
                        <GroupStyle HeaderTemplate="{StaticResource GroupHeaderTemplate}" />
                    </ListView.GroupStyle>
                </ListView>
            </SemanticZoom.ZoomedInView>
            <SemanticZoom.ZoomedOutView>
                <GridView
                    IsItemClickEnabled="True"
                    ItemTemplate="{StaticResource ZoomedOutGroupTemplate}"
                    ItemsSource="{Binding CollectionGroups, Source={StaticResource GroupedItems}}" />
            </SemanticZoom.ZoomedOutView>
        </SemanticZoom>
    </Grid>
</UserControl>
