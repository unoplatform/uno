<UserControl
    x:Class="UITests.Shared.Windows_UI_Xaml_Controls.ContentDialogTests.ContentDialog_Closing"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.ContentDialogTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <TextBlock Margin="50" Text="ContentDialog Closing tests" />
            <Button
                x:Name="AutoCloseDialog"
                Click="Button_Click"
                Content="Show auto-closing dialog" />
            <Button
                x:Name="DeferredDialog"
                Click="DeferredDialog_Click"
                Content="Show dialog with deferred close" />
            <Button
                x:Name="PrimaryDialog"
                Click="PrimaryDialog_Click"
                Content="Show dialog with Primary button" />
            <Button
                x:Name="PrimaryDialogCancelClosing"
                Click="PrimaryDialogCancelClosing_Click"
                Content="Show dialog with Primary button and cancel closing" />
            <TextBlock x:Name="ResultTextBlock" Margin="50" />
            <TextBlock
                x:Name="DidCloseTextBlock"
                Margin="50"
                Text="Not closed" />
        </StackPanel>
    </Grid>
</UserControl>
