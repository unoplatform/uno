<Page
    x:Class="UITests.Windows_UI_Xaml_Controls.Repeater.MyItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Controls.Repeater"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Border
        MinHeight="30"
        Margin="10"
        Padding="10"
        Background="{x:Bind GetColor(DataContext)}">
        <TextBlock FontSize="30">
            This is item #<Run Text="{Binding}" />
            which is using template #<Run Text="{x:Bind InstanceId}" />
            (of<Run Text="{x:Bind InstanceCount}" />
            )</TextBlock>
    </Border>
</Page>
