<Page
    x:Class="UITests.Windows_UI_Xaml_Controls.FrameTests.Frame_PageStacking"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml_Controls.Frame"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Background="Purple">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Tapped="HandleTappedOnNavBar">
            <Button
                x:Name="NavBack"
                Click="NavigateBack"
                Content="Back" />
            <Button
                x:Name="NavRed"
                Click="NavigateRed"
                Content="Nav to red page" />
            <Button
                x:Name="NavTransparent"
                Click="NavigateTransparent"
                Content="Nav to transparent page" />

            <TextBlock x:Name="TappedResult" Text="**not tapped**" />
        </StackPanel>

        <Frame x:Name="MyFrame" Grid.Row="1" />
    </Grid>
</Page>
