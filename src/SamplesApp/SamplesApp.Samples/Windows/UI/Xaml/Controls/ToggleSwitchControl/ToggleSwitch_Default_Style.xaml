<UserControl
    x:Class="SamplesApp.Windows_UI_Xaml_Controls.ToggleSwitchControl.ToggleSwitch_Default_Style"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Uno.UI.Samples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:not_win="http://uno.ui/not_win"
    xmlns:u="using:Uno.UI.Samples.Controls"
    xmlns:uBehaviors="using:Uno.UI.Samples.Behaviors"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    d:DesignHeight="2000"
    d:DesignWidth="400"
    mc:Ignorable="d not_win">

    <UserControl.Resources>
        <ResourceDictionary>

            <SolidColorBrush
                x:Key="LightGreyBrush"
                Opacity="0.38"
                Color="#000000" />

            <not_win:Style x:Key="UmbrellaToggleSwitchStyle" TargetType="ToggleSwitch">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleSwitch" />
                    </Setter.Value>
                </Setter>
            </not_win:Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <controls:SampleControl SampleDescription="ToggleSwitch_Default_Style - on Xamarin platforms this is styled with the native control. It should still work if the control is detached and reattached to the window.">
        <controls:SampleControl.SampleContent>
            <DataTemplate>
                <StackPanel>
                    <ToggleSwitch
                        not_win:Style="{StaticResource UmbrellaToggleSwitchStyle}"
                        Header="Sample Text"
                        IsOn="{Binding [IsOn], Mode=TwoWay}" />
                    <TextBlock Foreground="Blue">
                        <Run Text="(TextBlock) ToggleSwitch IsOn: " /><Run Text="{Binding [IsOn]}" />
                    </TextBlock>
                    <Button Command="{Binding [Flip]}" Content="Flip the Switch" />
                </StackPanel>
            </DataTemplate>
        </controls:SampleControl.SampleContent>
    </controls:SampleControl>
</UserControl>
