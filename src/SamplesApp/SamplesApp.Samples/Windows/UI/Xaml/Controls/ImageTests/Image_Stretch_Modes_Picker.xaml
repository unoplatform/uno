<UserControl
    x:Class="Uno.UI.Samples.UITests.Image.Image_Stretch_Modes_Picker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">


    <Grid Padding="20,10" ColumnSpacing="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ComboBox
            x:Name="stretchModes"
            Grid.Column="0"
            HorizontalAlignment="Stretch"
            Header="Stretch Mode:" />
        <Button
            x:Name="nextStretch"
            Grid.Column="1"
            Padding="0"
            VerticalAlignment="Bottom"
            Click="OnNextStretch">
            ⏩
        </Button>
        <ComboBox
            x:Name="horizontalModes"
            Grid.Column="2"
            HorizontalAlignment="Stretch"
            Header="Horizontal Alignment:" />
        <Button
            x:Name="nextHorizontal"
            Grid.Column="3"
            Padding="0"
            VerticalAlignment="Bottom"
            Click="OnNextHorizontal">
            ⏩
        </Button>
        <ComboBox
            x:Name="verticalModes"
            Grid.Column="4"
            HorizontalAlignment="Stretch"
            Header="Vertical Alignment:" />
        <Button
            x:Name="nextVertical"
            Grid.Column="5"
            Padding="0"
            VerticalAlignment="Bottom"
            Click="OnNextVertical">
            ⏩
        </Button>
        <StackPanel
            Grid.Column="6"
            VerticalAlignment="Bottom"
            Orientation="Horizontal"
            Spacing="5">
            <Button x:Name="previousMode" Click="OnPrevious">
                &lt; Previous
            </Button>
            <Button x:Name="currentMode" Click="OnCurrent">
                XX
            </Button>
            <Button x:Name="nextMode" Click="OnNext">
                Next &gt;
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
