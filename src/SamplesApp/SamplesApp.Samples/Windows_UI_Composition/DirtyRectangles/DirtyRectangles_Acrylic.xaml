<Page
    x:Class="UITests.Shared.Windows_UI_Composition.DirtyRectangles.DirtyRectangles_Acrylic"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <!--
        A translucent acrylic (backdrop-blur) panel over a small element that moves behind it and
        settles. Acrylic is a backdrop effect brush (RequiresRepaintOnEveryFrame) that samples the
        canvas well beyond its own bounds, so it stresses the dirty-rectangles damage region: if the
        damage doesn't cover the acrylic's sampling area (and the changed backdrop behind it), the
        blurred result goes stale. The settled end state must match a full-frame repaint.
    -->
    <Grid>
        <Rectangle Fill="DarkSlateBlue" />
        <Rectangle
            Width="240"
            Height="240"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Fill="Orange" />
        <Rectangle
            Width="240"
            Height="240"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Fill="MediumSpringGreen" />

        <Canvas>
            <Ellipse
                x:Name="Mover"
                Canvas.Left="0"
                Canvas.Top="200"
                Width="80"
                Height="80"
                Fill="Red" />
        </Canvas>

        <Border
            Width="320"
            Height="200"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Border.Background>
                <AcrylicBrush
                    FallbackColor="Gray"
                    TintColor="White"
                    TintOpacity="0.3" />
            </Border.Background>
        </Border>
    </Grid>
</Page>
