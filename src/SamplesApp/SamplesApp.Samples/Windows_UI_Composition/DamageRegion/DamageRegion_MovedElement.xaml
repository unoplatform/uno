<Page
    x:Class="UITests.Shared.Windows_UI_Composition.DamageRegion.DamageRegion_MovedElement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <!--
        A small element that moves across a static background on a timer. Used to verify
        that both the vacated (old) and new regions are repainted with no stale pixels,
        and that output stays identical to a full-frame repaint.
    -->
    <Canvas>
        <Border
            x:Name="Mover"
            Canvas.Left="0"
            Canvas.Top="100"
            Width="60"
            Height="60"
            Background="{ThemeResource SystemControlBackgroundAccentBrush}" />
    </Canvas>
</Page>
