<Page
    x:Class="UITests.Shared.Windows_UI_Composition.DamageRegion.DamageRegion_ShadowChildPaintingCaster"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:toolkit="using:Uno.UI.Toolkit"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Canvas>
        <!--
            A SELF-PAINTING shadow caster: it has its own (semi-transparent) Background, so CanPaint() is true,
            but it does not occlude its bounds, so the shadow silhouette is the union of the caster and its
            child. The child moves within the caster, changing the shadow even though the caster paints its own
            content and never moves. Exercises the self-painting-caster case the transparent ShadowChild sample
            doesn't. (The child stays inside the caster so the caster's shadow-damage bounds cover it.)
        -->
        <Border
            x:Name="Caster"
            Canvas.Left="120"
            Canvas.Top="120"
            Width="320"
            Height="200"
            toolkit:UIElementExtensions.Elevation="24"
            Background="#40808080">
            <Canvas>
                <Border
                    x:Name="Child"
                    Canvas.Left="0"
                    Canvas.Top="60"
                    Width="80"
                    Height="80"
                    Background="DarkSlateBlue" />
            </Canvas>
        </Border>
    </Canvas>
</Page>
