<Page
    x:Class="UITests.Shared.MessageDialogTests.MessageDialogTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.MessageDialog"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <Button
                x:Name="showDialogBtn"
                Grid.Row="0"
                Click="OnClick"
                Content="Show dialog" />
            <CheckBox
                x:Name="chkBox"
                Checked="OnCheckedChanged"
                Unchecked="OnCheckedChanged" />
        </StackPanel>
        <TextBlock
            x:Name="labelOutside"
            Grid.Row="1"
            Text="Text at bottom to be outside the bounds of the dialog" />
    </Grid>
</Page>
