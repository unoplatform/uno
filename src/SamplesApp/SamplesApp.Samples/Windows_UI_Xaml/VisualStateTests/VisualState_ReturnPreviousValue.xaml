<Page
    x:Class="UITests.Windows_UI_Xaml.VisualStateTests.VisualState_ReturnPreviousValue"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Windows_UI_Xaml.VisualStateTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid x:Name="RootGrid" Background="Green">
            <StackPanel Orientation="Vertical">
                <Button x:Name="ChangeBackgroundButton" Click="ChangeBackground_Click">
                    Change background (white)
                </Button>
                <Button
                    x:Name="DefaultStateButton"
                    Click="SetState_Click"
                    Tag="DefaultState">
                    Set default state (Green)
                </Button>
                <Button
                    x:Name="SecondStateButton"
                    Click="SetState_Click"
                    Tag="SecondState">
                    Set second state (Red)
                </Button>
                <Button
                    x:Name="ThirdStateButton"
                    Click="SetState_Click"
                    Tag="ThirdState">
                    Set third state (Blue)
                </Button>
            </StackPanel>
        </Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="DefaultState" />
                <VisualState x:Name="SecondState">
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Background" Value="Red" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ThirdState">
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Background" Value="Blue" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
