<UserControl
    x:Class="UITests.Shared.Windows_UI_Xaml.FrameworkElementTests.LoadEvents"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Uno.UI.Samples.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml.FrameworkElementTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock Margin="0,0,0,5" Text="This sample tests that Loaded and Unloaded events are raised properly. You should see ** 2 ** lines prefixed by [OK]." />

        <TextBlock FontWeight="Black" Text="Loaded:" />
        <TextBlock
            x:Name="loadedResult"
            Margin="0,5"
            Loaded="OnLoadTextLoaded"
            Text="[ERROR] loaded not received" />

        <TextBlock FontWeight="Black" Text="Unloaded:" />
        <TextBlock
            x:Name="unloadedResult"
            Margin="0,5"
            Loaded="OnUnloadTextLoaded"
            Text="[ERROR] loaded not received"
            Unloaded="OnUnloadTextUnloaded" />
    </StackPanel>
</UserControl>
