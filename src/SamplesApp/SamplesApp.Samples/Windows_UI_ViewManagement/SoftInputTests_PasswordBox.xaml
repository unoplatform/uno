<Page
	x:Class="UITests.Windows_UI_ViewManagement.SoftInputTests_PasswordBox"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	mc:Ignorable="d">

	<!-- Test: PasswordBox at bottom of page with NO ScrollViewer.
		 Expected: Same as NoScrollViewer test but with PasswordBox - RootScrollViewer should scroll into view. -->
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Padding="12" Spacing="4">
			<TextBlock Text="Soft Input: PasswordBox" FontSize="18" FontWeight="Bold" />
			<TextBlock Text="Tap the PasswordBox at the bottom. When the keyboard appears, the page should scroll up so the PasswordBox is visible above the keyboard." TextWrapping="Wrap" />
			<TextBlock x:Name="StatusText" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
		</StackPanel>

		<Border Grid.Row="1" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}" Margin="12,8">
			<TextBlock Text="(Spacer - no ScrollViewer wrapping this content)" HorizontalAlignment="Center" VerticalAlignment="Center" />
		</Border>

		<PasswordBox Grid.Row="2" PlaceholderText="Tap here to open keyboard..." Margin="12,8" />
	</Grid>
</Page>
