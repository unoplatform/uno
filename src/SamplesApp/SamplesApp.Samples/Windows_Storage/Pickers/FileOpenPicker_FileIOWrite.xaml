<Page
    x:Class="UITests.Shared.Windows_Storage.Pickers.FileOpenPicker_FileIOWrite"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <ScrollViewer
        MaxWidth="800"
        Padding="12,12,24,12"
        HorizontalContentAlignment="Left">
        <StackPanel Spacing="12">
            <TextBlock FontWeight="SemiBold" Text="FileOpenPicker and FileIO write" />
            <TextBlock Text="Use a disposable .txt file for this test. The sample replaces its contents with a known value." TextWrapping="WrapWholeWords" />
            <TextBlock Text="1. Select the file with the button below. 2. After the write completes, open the original file in the source location with File Explorer or the platform equivalent. 3. Verify that it contains: Uno Platform FileOpenPicker manual test." TextWrapping="WrapWholeWords" />
            <TextBlock Text="WebAssembly requires a browser that supports the File System Access API. The upload fallback cannot update the original source file." TextWrapping="WrapWholeWords" />
            <Button
                x:Name="PickAndModifyButton"
                Click="PickAndModifyButton_Click"
                Content="Pick a text file and modify it" />
            <TextBlock x:Name="StatusTextBlock" TextWrapping="WrapWholeWords" />
        </StackPanel>
    </ScrollViewer>
</Page>
