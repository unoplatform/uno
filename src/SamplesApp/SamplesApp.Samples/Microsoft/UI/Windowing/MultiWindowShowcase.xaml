<Page
    x:Class="UITests.Microsoft_UI_Windowing.MultiWindowShowcase"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Microsoft_UI_Windowing"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Padding="32" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="6">
            <TextBlock
                FontSize="30"
                FontWeight="SemiBold"
                Text="Multiple windows" />
            <TextBlock
                MaxWidth="460"
                HorizontalAlignment="Left"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="Every window is a Window of its own, with its own visual tree, its own render surface and its own input."
                TextWrapping="Wrap" />
        </StackPanel>

        <Button
            x:Name="OpenWindowButton"
            Grid.Row="1"
            Padding="20,12"
            HorizontalAlignment="Left"
            Click="OnOpenWindow"
            Content="Open a window"
            Style="{StaticResource AccentButtonStyle}" />

        <StackPanel
            x:Name="UnsupportedPanel"
            Grid.Row="2"
            Visibility="Collapsed">
            <TextBlock
                Foreground="{ThemeResource SystemFillColorCautionBrush}"
                Text="This target does not support multiple windows."
                TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>
