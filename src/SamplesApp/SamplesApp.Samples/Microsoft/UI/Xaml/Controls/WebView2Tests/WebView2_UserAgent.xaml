<Page
    x:Class="UITests.Microsoft_UI_Xaml_Controls.WebView2Tests.WebView2_UserAgent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Microsoft_UI_Xaml_Controls.WebView2Tests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="18"
            FontWeight="SemiBold"
            Text="CoreWebView2Settings.UserAgent" />

        <TextBlock
            Grid.Row="1"
            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
            Text="Sets the User-Agent string sent on HTTP requests and exposed via navigator.userAgent. The change applies to subsequent navigations. WebAssembly host is not supported (sandboxed iframe)."
            TextWrapping="Wrap" />

        <Grid Grid.Row="2" ColumnSpacing="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                x:Name="UserAgentInput"
                Grid.Column="0"
                PlaceholderText="MyApp/1.0 (custom)"
                Text="UnoSamplesAgent/1.0" />

            <Button
                x:Name="ApplyButton"
                Grid.Column="1"
                Click="OnApplyClick"
                Content="Apply" />

            <Button
                x:Name="ResetButton"
                Grid.Column="2"
                Click="OnResetClick"
                Content="Reset" />

            <Button
                x:Name="ShowButton"
                Grid.Column="3"
                Click="OnShowClick"
                Content="Show navigator.userAgent" />
        </Grid>

        <TextBlock
            x:Name="StatusText"
            Grid.Row="3"
            FontFamily="Consolas, Courier New, monospace"
            TextWrapping="Wrap" />

        <controls:WebView2
            x:Name="WebView"
            Grid.Row="4"
            Source="https://httpbin.org/user-agent" />
    </Grid>
</Page>
