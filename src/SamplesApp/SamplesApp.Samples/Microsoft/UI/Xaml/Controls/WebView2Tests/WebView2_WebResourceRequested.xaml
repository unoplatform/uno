<Page
    x:Class="UITests.Microsoft_UI_Xaml_Controls.WebViewTests.WebView2_WebResourceRequested"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UITests.Microsoft_UI_Xaml_Controls.WebViewTests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button x:Name="AddFilterButton" Content="Add wildcard filter (*)" />
            <Button x:Name="NavigateButton" Content="Navigate test page" />
            <TextBlock
                x:Name="StatusText"
                Margin="8,0,0,0"
                VerticalAlignment="Center" />
        </StackPanel>

        <controls:WebView2 x:Name="WebView" Grid.Row="1" />
    </Grid>

</Page>
