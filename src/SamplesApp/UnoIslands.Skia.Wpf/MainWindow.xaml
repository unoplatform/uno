<Window
    x:Class="UnoIslands.Wpf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xamlHost="clr-namespace:Uno.UI.XamlHost.Skia.Wpf;assembly=Uno.UI.XamlHost.Skia.Wpf"
    Title="Uno Islands Sample"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <ListView
            x:Name="List"
            ItemsSource="{Binding Data}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid
            x:Name="DetailsPane"
            Grid.Column="1"
            DataContext="{Binding SelectedItem}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <CheckBox
                    x:Name="contentLoaded"
                    Checked="contentLoaded_Checked"
                    Content="UnoXamlHost Loaded ?"
                    IsChecked="true"
                    Unchecked="contentLoaded_Unchecked" />
            </StackPanel>
            <ContentControl x:Name="hostContainer" Grid.Row="1">
                <xamlHost:UnoXamlHost x:Name="xamlHost" InitialTypeName="UnoIslands.MainPage" />
            </ContentControl>
        </Grid>
    </Grid>
</Window>
