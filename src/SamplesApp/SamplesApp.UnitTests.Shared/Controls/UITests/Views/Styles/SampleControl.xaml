<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Uno.UI.Samples.Controls"
    xmlns:not_win="http://uno.ui/not_win"
    xmlns:u="using:Uno.UI.Samples.Controls">
    <!--  Merged Dictionaries  -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="ms-appx:///Controls/UITests/Views/Styles/Application/_Application.xaml" />
        <ResourceDictionary Source="ms-appx:///Controls/UITests/Views/Styles/Controls/_Controls.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="DefaultSampleControlStyle" TargetType="controls:SampleControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:SampleControl">
                    <Border
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Background="{TemplateBinding Background}">
                        <u:StarStackPanel Orientation="Vertical">
                            <ContentControl
                                Padding="10,10,10,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"
                                u:StarStackPanel.Size="*"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding SampleContent}" />
                        </u:StarStackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultSampleControlStyle}" TargetType="controls:SampleControl" />

</ResourceDictionary>
