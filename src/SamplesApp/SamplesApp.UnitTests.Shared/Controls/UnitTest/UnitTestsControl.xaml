<UserControl
    x:Class="Uno.UI.Samples.Tests.UnitTestsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:not_skia="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:skia="http://platform.uno/skia"
    x:Name="UnitTestsRootControl"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d skia">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition x:Name="failedTestDetailsRow" Height="0" />
            <!--  Will be set to height = 100 if any exception  -->
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="outputColumn" Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Spacing="4">
            <Grid ColumnSpacing="4" RowSpacing="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button
                    x:Name="runButton"
                    Grid.Row="0"
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    Click="OnRunTests"
                    Style="{ThemeResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Play" />
                        <TextBlock Text="Run" />
                    </StackPanel>
                </Button>
                <Button
                    x:Name="stopButton"
                    Grid.Row="0"
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    Click="OnStopTests"
                    IsEnabled="False">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Stop" />
                        <TextBlock Text="Stop" />
                    </StackPanel>
                </Button>
                <Button Grid.Column="2">
                    <Button.Content>
                        <SymbolIcon Symbol="Setting" />
                    </Button.Content>
                    <Button.Flyout>
                        <Flyout>
                            <StackPanel Width="320" Spacing="8">
                                <!--  Options: 2x2 grid of toggles  -->
                                <TextBlock
                                    FontWeight="SemiBold"
                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Text="OPTIONS" />
                                <Grid
                                    HorizontalAlignment="Stretch"
                                    ColumnSpacing="4"
                                    RowSpacing="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <ToggleButton
                                        x:Name="consoleOutput"
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        ToolTipService.ToolTip="Mirror test output to the debug console">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon FontSize="14" Glyph="&#xED0D;" />
                                            <TextBlock Text="Console" />
                                        </StackPanel>
                                    </ToggleButton>
                                    <ToggleButton
                                        x:Name="runIgnored"
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        ToolTipService.ToolTip="Include tests marked with [Ignore] attribute">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon FontSize="14" Glyph="&#xEE35;" />
                                            <TextBlock Text="Run ignored" />
                                        </StackPanel>
                                    </ToggleButton>
                                    <ToggleButton
                                        x:Name="retry"
                                        Grid.Row="1"
                                        Grid.Column="0"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        IsChecked="True"
                                        ToolTipService.ToolTip="Automatically retry failed tests">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon FontSize="14" Glyph="&#xEC57;" />
                                            <TextBlock Text="Auto retry" />
                                        </StackPanel>
                                    </ToggleButton>
                                    <ToggleButton
                                        x:Name="unloadTestContent"
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        IsChecked="True"
                                        ToolTipService.ToolTip="Remove test UI content from the visual tree after each test completes">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon FontSize="14" Glyph="&#xE74D;" />
                                            <TextBlock Text="Content unload" />
                                        </StackPanel>
                                    </ToggleButton>
                                </Grid>

                                <!--  Divider  -->
                                <Rectangle
                                    Height="1"
                                    HorizontalAlignment="Stretch"
                                    Fill="{ThemeResource SystemControlForegroundBaseLowBrush}" />

                                <!--  Repeat + Test Grouping: side by side  -->
                                <Grid HorizontalAlignment="Stretch" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <!--  Left column: Repeat  -->
                                    <StackPanel
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        Spacing="4">
                                        <TextBlock
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                            Style="{ThemeResource CaptionTextBlockStyle}"
                                            Text="REPEAT" />
                                        <TextBox
                                            x:Name="iterationCount"
                                            HorizontalAlignment="Stretch"
                                            Header="Iterations"
                                            InputScope="Number"
                                            PlaceholderText="1"
                                            ToolTipService.ToolTip="Number of times to repeat each test case (can be used to validate repeatability)" />
                                    </StackPanel>

                                    <!--  Right column: Test Grouping  -->
                                    <StackPanel
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        Spacing="4">
                                        <TextBlock
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                            Style="{ThemeResource CaptionTextBlockStyle}"
                                            Text="TEST GROUPING" />
                                        <TextBox
                                            x:Name="testGroupNumber"
                                            HorizontalAlignment="Stretch"
                                            Header="Group ID"
                                            InputScope="Number"
                                            PlaceholderText="e.g. 0"
                                            ToolTipService.ToolTip="Zero-based index of the test group to run" />
                                        <TextBox
                                            x:Name="testGroupCount"
                                            HorizontalAlignment="Stretch"
                                            Header="Group count"
                                            InputScope="Number"
                                            PlaceholderText="e.g. 12"
                                            ToolTipService.ToolTip="Total number of groups to split tests into" />
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>

                <TextBox
                    x:Name="testFilter"
                    Grid.Row="1"
                    Grid.ColumnSpan="3"
                    HorizontalAlignment="Stretch"
                    PlaceholderText="Enter test filter here" />
            </Grid>

            <StackPanel Margin="0,8" Orientation="Horizontal">
                <TextBlock Text="Status: " />
                <TextBlock x:Name="runStatus" Text="Not initialized" />
            </StackPanel>

            <Grid Margin="8" RowSpacing="8">
                <Grid.Resources>
                    <Style TargetType="StackPanel">
                        <Setter Property="HorizontalAlignment" Value="Center" />
                        <Setter Property="Spacing" Value="4" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Orientation" Value="Horizontal" />
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="Width" Value="40" />
                    </Style>
                    <Style TargetType="FontIcon">
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel ToolTipService.ToolTip="Total tests">
                    <FontIcon Glyph="&#xE768;" />
                    <TextBlock x:Name="runTestCount" Text="None" />
                </StackPanel>

                <StackPanel Grid.Column="1" ToolTipService.ToolTip="Succeeded tests">
                    <FontIcon Foreground="DarkGreen" Glyph="&#xE73E;" />
                    <TextBlock
                        x:Name="succeededTestCount"
                        Foreground="DarkGreen"
                        Text="None" />
                </StackPanel>

                <StackPanel Grid.Column="2" ToolTipService.ToolTip="Failed tests">
                    <FontIcon Foreground="Red" Glyph="&#xE711;" />
                    <TextBlock
                        x:Name="failedTestCount"
                        Foreground="Red"
                        Text="None" />
                </StackPanel>

                <StackPanel
                    Grid.Row="1"
                    Grid.Column="0"
                    ToolTipService.ToolTip="Inconclusive tests">
                    <FontIcon Foreground="DarkViolet" Glyph="&#xE9CE;" />
                    <TextBlock
                        x:Name="inconclusiveTestCount"
                        Foreground="DarkViolet"
                        Text="None" />
                </StackPanel>

                <StackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    ToolTipService.ToolTip="Retried tests">
                    <FontIcon Foreground="Orange" Glyph="&#xE8EE;" />
                    <TextBlock
                        x:Name="retriedTestCount"
                        Foreground="Orange"
                        Text="None" />
                </StackPanel>

                <StackPanel
                    Grid.Row="1"
                    Grid.Column="2"
                    ToolTipService.ToolTip="Ignored tests">
                    <FontIcon Foreground="DarkGray" Glyph="&#xEE35;" />
                    <TextBlock
                        x:Name="ignoredTestCount"
                        Foreground="DarkGray"
                        Text="None" />
                </StackPanel>
            </Grid>
        </StackPanel>

        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Auto"
            HorizontalScrollMode="Auto"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <TextBlock
                x:Name="failedTestDetails"
                IsTextSelectionEnabled="True"
                Opacity="0.8" />
        </ScrollViewer>
        <Button
            Grid.Row="1"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Click="CopyFailedTestDetails">
            <FontIcon Glyph="&#xE8C8;" />
        </Button>

        <Rectangle
            Grid.Row="2"
            Height="5"
            HorizontalAlignment="Stretch"
            Fill="{ThemeResource SystemControlForegroundAccentBrush}"
            ManipulationDelta="UpdateFailedTestDetailsSize"
            ManipulationMode="TranslateY" />

        <TextBlock x:Name="failedTests" Grid.Row="3" />
        <TextBlock x:Name="runningState" Grid.Row="3" />
        <ScrollViewer x:Name="testResultsScroller" Grid.Row="3">
            <StackPanel x:Name="testResults" Background="Black" />
        </ScrollViewer>
        <Button
            Grid.Row="3"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Click="CopyTestResults">
            <FontIcon Glyph="&#xE8C8;" />
        </Button>

        <Rectangle
            Grid.RowSpan="4"
            Grid.Column="1"
            Width="5"
            VerticalAlignment="Stretch"
            Fill="{ThemeResource SystemControlForegroundAccentBrush}"
            ManipulationDelta="UpdateOuputSize"
            ManipulationMode="TranslateX" />

        <ContentControl
            x:Name="unitTestContentRoot"
            Grid.RowSpan="4"
            Grid.Column="2"
            VerticalAlignment="Top"
            HorizontalContentAlignment="Left" />
    </Grid>
</UserControl>
