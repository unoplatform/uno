<Page
    x:Class="FrameworkPoolEditorRecycling.Editors.EditorXBindPropertyChangedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FrameworkPoolEditorRecycling.Editors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <StackPanel>
        <TextBox Text="{x:Bind ViewModel.Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		<CheckBox IsChecked="{x:Bind ViewModel.IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		<ToggleSwitch IsOn="{x:Bind ViewModel.IsOn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
	</StackPanel>
</Page>
