<Page x:Class="Uno.UI.RuntimeTests.Tests.Windows_UI_Xaml_Controls.ContentPresenterPages.ContentPresenter_ContentBindingPath"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  mc:Ignorable="d"
	  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<ListView x:Name="SetupLV">
		<ListView.ItemTemplate>
			<DataTemplate>
				<!--<Border>-->
					<ContentPresenter x:Name="SutContentPresenter" Content="{Binding Value}" Foreground="Black"/>
				<!--</Border>-->
				<!--<StackPanel>
					<ContentPresenter x:Name="SutContentPresenter" Content="{Binding Value}" />
					<ContentControl x:Name="RefContentControl" Content="{Binding Value}" />
				</StackPanel>-->
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</Page>
