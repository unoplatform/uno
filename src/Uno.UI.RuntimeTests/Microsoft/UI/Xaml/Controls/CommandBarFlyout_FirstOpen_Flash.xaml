<UserControl
	x:Class="Uno.UI.RuntimeTests.Tests.Windows_UI_Xaml_Controls.CommandBarFlyout_FirstOpen_Flash"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Uno.UI.RuntimeTests.Tests.Windows_UI_Xaml_Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<!--
	Compiled-XAML repro of the kahua-private #480 first-open flash on Skia.

	Unlike Given_CommandBarFlyout's XamlReader.Load test (which exercises the
	EAGER ResourceResolver.ApplyResource path, immediateResolution=false, and is
	fully covered by the parse-time dictionary pinning), this page is COMPILED
	XAML. Compiled {ThemeResource} assignments take the DEFERRED branch
	(immediateResolution=true) — an unpinned ThemeResourceReference resolved by
	the load-time _resourceBindings tree walk — which is the path Kahua actually
	ships and the one still reported to flash.

	RequestedTheme=Light on the root while the application/OS theme is Dark
	(set by the test) reproduces Kahua's ThemeAssist model (element-pinned Light
	under ambient Dark). MenuItemBrush is declared in the root's own
	ThemeDictionaries: Light=Green (correct), Dark=Red (the wrong-theme flash).
	-->
	<Border x:Name="Root" RequestedTheme="Light">
		<Border.Resources>
			<ResourceDictionary>
				<ResourceDictionary.ThemeDictionaries>
					<ResourceDictionary x:Key="Light">
						<SolidColorBrush x:Key="MenuItemBrush" Color="Green" />
					</ResourceDictionary>
					<ResourceDictionary x:Key="Dark">
						<SolidColorBrush x:Key="MenuItemBrush" Color="Red" />
					</ResourceDictionary>
				</ResourceDictionary.ThemeDictionaries>
			</ResourceDictionary>
		</Border.Resources>

		<Button x:Name="Owner" Content="Owner">
			<Button.Flyout>
				<CommandBarFlyout>
					<CommandBarFlyout.PrimaryCommands>
						<AppBarButton x:Name="Command"
								Label="Item"
								Foreground="{ThemeResource MenuItemBrush}" />
					</CommandBarFlyout.PrimaryCommands>
				</CommandBarFlyout>
			</Button.Flyout>
		</Button>
	</Border>
</UserControl>
