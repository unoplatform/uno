<Page
	x:Class="Uno.UI.RuntimeTests.Tests.Windows_UI_Xaml.Controls.Style_Override"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Uno.UI.RuntimeTests.Tests.Windows_UI_Xaml.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Page.Resources>
		<!-- Style for testing Without BasedOn -->
		<Style x:Key="StyleWithoutBasedOn"
		       TargetType="Button">
			<Setter Property="CornerRadius" Value="8" />
			<Setter Property="Background" Value="Red" />
		</Style>

		<!-- Style for testing With BasedOn -->
		<Style x:Key="StyleWithBasedOn"
		       TargetType="Button"
		       BasedOn="{StaticResource DefaultButtonStyle}">
			<Setter Property="CornerRadius" Value="8" />
			<Setter Property="Background" Value="Red" />
		</Style>

		<!-- App-level style -->
		<Style x:Key="AppLevelButtonStyle"
		       TargetType="Button">
			<Setter Property="CornerRadius" Value="4" />
			<Setter Property="Background" Value="Green" />
		</Style>
	</Page.Resources>

	<Grid>
		<StackPanel>
			<Button x:Name="buttonWithoutBasedOn"
			        Content="Without BasedOn"
			        Style="{StaticResource StyleWithoutBasedOn}" />
			<Button x:Name="buttonWithBasedOn"
			        Content="With BasedOn"
			        Style="{StaticResource StyleWithBasedOn}" />
		</StackPanel>
	</Grid>
</Page>
