parameters:
  vmImage: ''

jobs:
- job: Commitsar
  displayName: 'Conventional Commits Validation'

  container:
    image: aevea/commitsar:0.16.0
    options: -it

  pool:
    vmImage: ${{ parameters.vmImage }}

  steps:
  - checkout: self
    clean: true

  - bash: commitsar
