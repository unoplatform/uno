parameters:
  vmLinuxPool: ''
  UNO_UWP_BUILD: ''
  XAML_FLAVOR_BUILD: ''

# GATED OFF FOR V1.
# CoreCLR-on-Android with BDN 0.14 needs local verification before turn-on.
# Enable in a follow-up PR.

jobs:
- job: Skia_Android_CoreCLR_Benchmarks
  displayName: 'Benchmarks - Skia Android (CoreCLR, gated off)'
  condition: false
  timeoutInMinutes: 30
  cancelTimeoutInMinutes: 0
  pool: ${{ parameters.vmLinuxPool }}
  steps:
  - bash: echo "Stage gated off in v1. See doc/benchmarks/provisioning.md."
    displayName: Gated stage placeholder
